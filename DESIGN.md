# ドラゴンボールレジェンズ フラグメント最適化アプリ — 設計仕様

このファイルは実装の引き継ぎ文書です。作業前に必ず全文を読んでください。
以降のセッションでもこのファイルを参照し、設計判断を変更した場合はここを更新してください。

---

## 0. このプロジェクトが何か

ドラゴンボールレジェンズ（以下 DBL）で、**所持しているフラグメントを
どのキャラにどう配分すれば最終ステータスが最大になるか**を計算する個人用アプリ。

- 利用者は1人（作者本人）。マルチユーザー対応・認証・サーバ不要
- iPhone のホーム画面にアイコンを置いてタップで開ける形にする
- 目標は「勘で決めていたフラグメント配分を、数式で確定させる」こと

### やらないこと（重要）

- **ゲーム本体からのデータ抽出（データマイニング）は実装しない。**
  アセットバンドルの復号、APK 解析、通信の傍受などは一切行わない。
  必要なデータは既存の公開データベースサイト、または手入力で賄う。
- 不特定多数への公開を前提とした作りにしない。
  （v2.14 で作者の要望により「仲間内での利用」までは想定に含めた — §26-2。
  　マルチユーザー機能・認証・サーバは引き続き作らない。データは各端末ローカルのまま）

---

## 1. 設計原則（変更しないこと）

この5つは過去の検討で確定した方針です。実装が面倒でも曲げないでください。

### 1-1. 外部データを依存にしない

外部サイトからの取り込みは**初期入力の補助機能**に過ぎない。
正のデータはすべてローカルに保存され、取り込みが壊れてもアプリは動作し続ける。
どの項目にも**手入力でのオーバーライド経路**を必ず用意すること。

### 1-2. IDで参照する

キャラ・タグ・フラグメントは内部ID（例: `character/738`, タグ `50010`）で参照する。
表示名や日本語文字列での照合は絶対にしない。翻訳や表記ゆれで壊れるため。

タグIDと名前の対応表は、参照サイトのキャラ一覧ページの `<select id="filterTAGS">`
に丸ごと含まれているので、そこから取得できる。

### 1-3. 変換表はコードではなくデータに置く

効果文言 → 数値の変換は YAML/JSON の外部ファイルに置く。
コード内に `if effect == "基礎打撃攻撃力アップ"` のような分岐を書かない。
新しい効果が追加されたら**ファイルに1行足すだけ**で対応できる状態を保つ。

### 1-4. 未知の語は絶対に 0 として扱わない

最悪の失敗は「落ちること」ではなく「**未知の効果を黙って無視して、
間違った最適解を自信満々に出すこと**」。

未知の効果文言・レアリティ・条件種別に遭遇したら、
計算結果と一緒に「未対応項目リスト」を必ず画面に表示する。

### 1-5. ゲームデータと自分のデータを分離する

- `game_data/` … 外部由来。いつ消して再取得してもよい
- `my_data/` … 所持フラグメント、キャラの星・ソウルブースト状況。消したら復旧不能

`my_data` は `game_data` の ID を参照するだけにし、ゲーム側の更新で無効化されないようにする。

---

## 2. ステータス計算モデル（検証済み・これが本体）

DBL のステータス計算式。実際のゲーム画面の数値で検算済み。

### 2-1. 用語

| 用語 | 意味 |
|---|---|
| 合計ステ | ゲームのステータス画面で左に出る数値（ブースト込み） |
| ブースト値 | 同画面で右に括弧付きで出る数値。ソウルブースト等による増加分 |
| 基礎あり補正 | 効果名に「基礎」が付く補正（例:「基礎打撃攻撃力アップ」）。加算プールに入る |
| 基礎なし補正 | 「基礎」が付かない補正（例:「打撃攻撃力アップ」）。最後に乗算される |

**パース規則: 効果名が「基礎」で始まるかどうかが、加算か乗算かの唯一の判別基準。**

### 2-2. 補正の適用範囲

| 補正源 | 誰の分が乗るか |
|---|---|
| Zアビリティ | **パーティ6体全員** |
| ZENKAIアビリティ | **パーティ6体全員** |
| LLアビリティ（LEGENDS LIMITED） | **バトル出撃する3体のみ** |
| フラグメント | 装備した本人のみ |

Zアビリティの数値は**限界突破（星）の数で変動する**。これはユーザー入力項目。

### 2-3. 計算式

```
❶ 基本ステータス
   = 合計ステ − ブースト値

❷ 基礎ステータス補正 (+%)
   = Σ(パーティ6体のZアビ補正)
   + Σ(パーティ6体のZENKAIアビ補正)
   + Σ(バトル3体のLLアビ補正)
   + Σ(装備フラグメントの「基礎あり」補正)

❸ 最終ステータス  ★これが最適化の目的関数
   = { (❷ × 0.01 + 1) × ❶ + ブースト値 }
     × ( Σ(装備フラグメントの「基礎なし」補正) × 0.01 + 1 )

❹ ブースト倍率
   = ブースト値 ÷ ❶

❺ 最終ステータス補正 (+%)   ※表示用
   = ( { ❷ × 0.01 + 1 + ❹ } × { 基礎なし補正 × 0.01 + 1 } − 1 ) × 100

❻ 合計フラグメント補正 (+%)  ※表示用
   = ❺ − (Zアビ + ZENKAIアビ + LLアビの補正合計) − (❹ × 100)
```

❻は「基礎なし補正を基礎あり相当に換算した値」で、
**フラグメント構成同士を1つの数値で比較するのに使える**。UIではこれを主に表示する。

### 2-4. 検算用テストケース（必ず自動テストにすること）

対象: 超サイヤ人4 孫悟空 (DBL81-04S) の打撃攻撃力

```
合計ステ      = 273,617
ブースト値    =  42,080
❶            = 231,537

Zアビ合計     = +149%   (パーティ6体)
ZENKAIアビ    =    0%
LLアビ合計    =  +30%   (バトル3体)
```

| ケース | 基礎あり | 基礎なし | ❷ | ❸(期待値) | ❺ | ❻ |
|---|---|---|---|---|---|---|
| (1) | +110% | 0% | +289% | **942,758.93** | 約 +307% | 約 +110% |
| (2) | +60% | +30% | +239% | **1,075,087.56** | 約 +364% | 約 +167% |
| (3) | +15% | +40% | +194% | **1,011,918.29** | 約 +337% | 約 +140% |

❹ = 42,080 ÷ 231,537 = 0.18173...

これらが小数第2位まで一致することをテストで担保すること。

### 2-5. 重要な性質（最適化ロジックの前提）

1ポイントあたりの限界価値は以下の通り。

```
基礎あり +1%  →  0.01 × ❶ × (1 + 基礎なし/100)
基礎なし +1%  →  0.01 × { (❷ × 0.01 + 1) × ❶ + ブースト値 }
```

比を取ると:

```
基礎なし1% の価値 ÷ 基礎あり1% の価値 = (1 + ❷/100) ÷ (1 + 基礎なし/100)
```

❷ は実戦で 150〜250% 程度、基礎なしは 0〜50% 程度なので、
**通常は基礎なしが 2〜3.5 倍効率が良い**。上のテストケース(2)では約 2.75 倍。

ただしこの比率は ❷（＝パーティ編成と星の数）に依存して変動するため、
**「基礎あり優先」「基礎なし優先」のような固定ルールは実装しないこと。**
必ず ❸ を直接評価して比較する。

> 参考にした元メモには「基礎ありを増やす方がステが高くなる」という記述があるが、
> 同メモ内の数値では基礎あり3枚(1)が3ケース中最下位であり、記述と数値が矛盾している。
> **数値の方が正しい。**

### 2-6. 未確定事項（実装時に実機で検証すること）

- 複数フラグメントの「基礎なし」補正は**加算してから1回だけ乗算**すると仮定している。
  各フラグメントごとに乗算する可能性も理論上あるので、実機の数値で確認すること。
- 端数処理（切り捨て/四捨五入）のタイミングが不明。現状は丸めなしで計算し、表示時のみ丸める。
- ❻ の各項の符号・順序は元メモの記法に従っている。

---

## 3. データモデル

### 3-1. `game_data/characters.json`

```json
{
  "738": {
    "id": 738,
    "card_no": "DBL98-01L",
    "name": "超サイヤ人ゴッドSS界王拳 孫悟空",
    "element": "PUR",
    "rarity": "LEGEND",
    "tags": [7, 1, 40, 45, 56, 9140, 10000, 12004, 13002, 15002, 20018, 50010],
    "base_stats": {
      "hp": 0, "strike_atk": 0, "blast_atk": 0,
      "strike_def": 0, "blast_def": 0, "critical": 0, "ki_recovery": 0
    }
  }
}
```

`base_stats` は Lv5000 の限界ステータス（合計ステ）。ブースト値は個人差があるので `my_data` 側。

### 3-2. `game_data/fragments.json`

```json
{
  "32012": {
    "id": 32012,
    "name": "【覚醒】再び立つか オレの前に!",
    "rarity": "...",
    "equip_conditions": { "require_tags_any": [50010], "require_tags_all": [] },
    "effects": [
      { "stat": "strike_atk", "base": true,  "value": 110 },
      { "stat": "strike_def", "base": false, "value": 20 }
    ]
  }
}
```

- `base: true` = 基礎あり（❷ に加算）
- `base: false` = 基礎なし（最後に乗算）
- 数値は**最大値**を入れる。所持個体ごとの厳選差は扱わない（下記 3-4 参照）

### 3-3. `game_data/effect_map.yaml`

効果文言 → 内部表現の変換表。**新効果はここに1行足すだけで対応する。**

```yaml
"基礎打撃攻撃力アップ":   { stat: strike_atk, base: true }
"打撃攻撃力アップ":       { stat: strike_atk, base: false }
"基礎射撃攻撃力アップ":   { stat: blast_atk,  base: true }
"射撃攻撃力アップ":       { stat: blast_atk,  base: false }
"基礎体力アップ":         { stat: hp,         base: true }
# ...
```

ここに存在しない文言は**未対応として記録し、計算から除外した旨を必ず表示する**（原則 1-4）。

### 3-4. `my_data/inventory.json`

```json
{
  "fragments": { "32012": 2, "1580": 1 },
  "characters": {
    "738": {
      "stars": 7,
      "equip_slots": 3,
      "boost": {
        "hp": 331630, "strike_atk": 42080, "blast_atk": 41271,
        "strike_def": 38817, "blast_def": 37974, "critical": 2242, "ki_recovery": 0
      },
      "z_ability": [ { "stat": "strike_atk", "base": true, "value": 30, "condition_tags": [7] } ],
      "ll_ability": [],
      "zenkai_ability": []
    }
  }
}
```

- `fragments` は ID → 所持個数。**個体差は扱わない**（所持品は基本すべて最大値のため）
- `equip_slots` はキャラごとに異なる（ソウルブーストで増える）。ゲーム画面の「装備枠」の合計値を入れる
- `stars`（限界突破数）で Zアビリティの数値が変わるため、`z_ability` の値はユーザーが実機を見て入力する

---

## 4. 最適化

### 4-1. 問題の形

- パーティ = 6体。うちバトル出撃 = 上位3体
- 各キャラは `equip_slots` 枚（通常3枚）のフラグメントを装備
- 同じフラグメント個体は同時に1体にしか装備できない（所持個数まで）
- 装備条件（タグ）を満たすフラグメントのみ装備可能

典型的な**割当問題**。組合せ爆発するので総当たりは避け、ILP で解く。

### 4-2. 実装

- Python なら OR-Tools (`CP-SAT`) または PuLP
- JavaScript なら `javascript-lp-solver`、または規模が小さいので貪欲法＋局所探索でも可

### 4-3. 目的関数の注意

❷ は**パーティ全体の合算値**なので、あるキャラのフラグメント選択が
他キャラの ❸ に影響することは**ない**（フラグメントは装備者本人にしか乗らない）。

したがって:

- **Zアビ/ZENKAIアビ/LLアビの合計（❷ のフラグメント以外の部分）は編成が決まれば定数**
- その定数を所与とすれば、**キャラごとに独立して最適化できる**
- ただしフラグメントの所持数が有限なので、**キャラ間で奪い合いが発生する**。ここだけが結合

→ よって「❷ の定数部分を先に計算 → フラグメント割当のみを ILP で解く」という2段構成にする。

### 4-4. スコアの重み付け

複数ステータスを同時に最大化はできないので、ユーザーが用途を選べるようにする。

- 攻撃特化 / 耐久特化 / バランス のプリセット
- 各ステータスの重みを手動調整できるスライダー
- 上級者向けに「実効HP × 期待ダメージ」の積を最大化するモードも検討（v2以降）

v0 は**単一ステータス最大化**（例: 打撃攻撃力）から始めること。

---

## 5. データ取り込み

### 5-1. 方針

「取り込み」ボタンによる**半自動更新**。全キャラは取得せず、
**ユーザーが登録したキャラだけを対象に取得**する（所持は数十体程度のため）。

取得したデータはローカルに永続キャッシュし、明示的に更新を押すまで再取得しない。

### 5-2. CORS の罠（重要）

ブラウザで動く PWA から外部サイトを直接 `fetch()` すると
**CORS で必ずブロックされる**。以下のいずれかで回避すること。

1. **推奨:** 更新用スクリプト（Node/Python）を別に用意し、
   生成した JSON をアプリの配信元に置く。アプリは自分と同一オリジンの
   `data.json` を読むだけなので CORS が発生しない
2. GitHub Pages 配信なら、GitHub Actions で定期的に取得＆コミットする構成も可
3. どうしても端末単独で完結させたい場合は、
   「ページのHTMLを貼り付けてパースする」入力欄を用意する（最終手段）

いずれの場合も、リクエスト間隔は空けること（連続アクセスで弾かれるのを防ぐため）。

### 5-3. パース対象

参照サイトのキャラ一覧ページは `data-*` 属性に構造化済みなので、
一覧のパースは容易。

```html
<a href="character/738"
   data-charaname="超サイヤ人ゴッドSS界王拳 孫悟空"
   data-element="PUR" data-rarity="LEGEND"
   data-zenkai="0" data-lf="0"
   data-tags="7 1 40 45 56 9140 10000 12004 13002 15002 20018 50010">
```

タグID→名前の対応表は同ページの `<select id="filterTAGS">` から取得。

ステータスの詳細やフラグメント効果は個別ページの取得が必要。
**HTML 構造は変わりうるので、パーサは独立したモジュールに隔離し、差し替えやすくすること。**

### 5-4. スナップショット保存

取得した生 HTML / JSON を日付付きでローカルに保存する。
パーサが壊れたときに差分で原因を特定でき、修正後に再パースできる。

---

## 6. エラー処理方針

**すべてのエラーメッセージは日本語で、原因と対処が分かる文言にすること。**
スタックトレースをそのまま出さない。

### メッセージ例

```
■ 取り込みに失敗しました
  参照先のページ構造が変わった可能性があります。
  アプリの更新が必要です。
  （前回取り込んだデータで引き続き利用できます）

■ 未対応の効果が 2 件ありました
  ・「基礎必殺技与ダメージアップ」（フラグメント: ○○）
  ・「気力回復速度アップ」（フラグメント: △△）
  これらは計算に含まれていません。実際の数値とズレる可能性があります。
  アプリの更新が必要です。

■ 未知のレアリティ「○○」を検出しました
  計算を中止しました。アプリの更新が必要です。
```

### 原則

- 取り込みが失敗しても、**キャッシュ済みデータでアプリは動き続ける**こと
- 未対応項目があっても計算は実行するが、**必ず警告を表示する**（黙って0にしない）
- レアリティなど計算の根幹に関わる未知の値は、計算を中止する

---

## 7. 配信形態

### 推奨: PWA（Progressive Web App）

iPhone のホーム画面にアイコンを置く要件を、最小コストで満たせる。

- `manifest.json` に `display: "standalone"` を設定
- Safari で開いて「ホーム画面に追加」→ ネイティブアプリのように起動する
- Service Worker でオフライン動作させる
- Xcode も Apple Developer アカウントも不要

### 配信方法

- GitHub Pages（無料・HTTPS・PWA 要件を満たす）
- またはローカルネットワーク内の静的サーバ

### データ保存

- IndexedDB を使用（localStorage は容量が心許ない）
- **必ず JSON エクスポート/インポート機能を付ける。** `my_data` は復旧不能なため

---

## 8. 実装順序

各段階で動くものを作り、次に進む前に実機の数値で検証すること。

### v0 — 計算エンジンの検証

- ステータス計算式（§2-3）の実装
- §2-4 のテストケースを自動テスト化
- キャラ1体・フラグメント手入力・単一ステータスで、❸ と ❻ を表示するだけ

**ここで実機の数値と一致することを確認できなければ、先に進まない。**

### v1 — 最適化

- 所持フラグメントの登録UI
- 1キャラに対する最適な N 枚を選ぶソルバ
- 装備条件（タグ）によるフィルタ

### v2 — パーティ全体

- パーティ6体・バトル3体の編成UI
- Zアビ/ZENKAIアビ/LLアビの合算
- フラグメントの奪い合いを含めた全体最適化（ILP）

### v3 — PWA 化と取り込み

- ホーム画面追加対応
- 取り込みボタンと更新スクリプト
- エラー表示の整備

### v4 — 以降

- 複数ステータスの重み付け最適化
- 「実効HP × 期待ダメージ」モード
- 構成の保存・比較

---

## 9. 実装開始前に確認すること

以下は未決定。作業を始める前にユーザーに確認すること。

1. **実装言語 / フレームワーク**
   PWA 前提なら素の HTML+JS か、React などを使うか
2. **想定用途**
   PvP か、イベント周回か（v4 のスコア重み付けに影響）
3. **最初に登録するキャラ**
   v0 の検証に使う実機データ（合計ステとブースト値）を用意してもらう

---

## 10. 実装記録（v0〜v2 実装時の決定事項）

§9 の未決定事項と、実装中に置いた仮定の記録。設計判断を変えた場合はここも更新すること。

### 10-1. 決定した事項

1. **実装言語**: 素の HTML + JS（ES Modules）。ビルド不要で GitHub Pages に直接置ける。
   テストは Node 標準の `node --test`（`npm test`）。依存パッケージはゼロ。
2. **最適化の実装**（§4-2）: 外部ソルバは使わず、キャラごとの装備組合せを全列挙して
   スコア降順に並べ、**貪欲法で初期解 → 分枝限定法で厳密解**を探索する（`js/optimizer.js`）。
   組合せ数・探索ノード数が上限を超えた場合は打ち切り、結果に「暫定解」と明示する。
3. **複数ステータスの重み付け**（§4-4）: スコア = **Σ 重み × ❸（絶対値）**。
   当初は相対値 ❸/❸₀ だったが、クリティカル等の絶対値が小さいステータスの
   +X% を過大評価して弱フラグを選ぶため v2.5 で絶対値に変更（§17）。
   プリセットの重みはステータスの桁を補正した値で定義する。
   単一ステータス最大化は one-hot の重みと同値（v0 要件を包含）。
4. **v0 の検算**: §2-4 の3ケースを `test/calc.test.mjs` で自動テスト化済み（小数第2位まで一致）。
   UI の「計算」タブにも検算(1)(2)(3) ボタンがあり、実機と突き合わせられる。

### 10-2. 実装中に置いた仮定（実機で要検証）

- **同一フラグメントの同キャラへの重複装備は不可**と仮定（既定値）。
  最適化オプションで許可に切り替え可能。
- **LLアビリティの発生源はパーティ6体の誰でもよく、効果はバトル3体にのみ乗る**と解釈
  （§2-2 の表の素直な読み）。LLキャラ自身がバトル出撃している必要があるかは未確認。
- **基礎なし（base:false）のアビリティ**は §2-3 の式に存在しないため、
  入力された場合は基礎なし補正として乗算側に合流させ、未検証である旨を警告する。
- レアリティ既知リスト（`game_data/config.json` の `known_rarities`）は空で出荷。
  空の間はチェックせず、実データ取り込み後に値を追加してから §6 の中止動作が有効になる。

### 10-3. ファイル対応表

| 設計 | 実装 |
|---|---|
| §2 計算式 | `js/calc.js` + `test/calc.test.mjs` |
| §3-3 変換表 | `game_data/effect_map.json`（JSON採用。YAMLパーサ不要のため） + `js/effects.js` |
| §4 最適化 | `js/optimizer.js` + `test/optimizer.test.mjs` |
| §5 取り込み | `tools/update_data.mjs`（Node・一覧＋タグ表対応）/ アプリ内「HTML貼り付け取り込み」 / `js/parser.js` |
| §5-4 スナップショット | `game_data/snapshots/<日付>/`（gitignore 済み） |
| §6 エラー処理 | 各所・すべて日本語文言 |
| §7 PWA | `manifest.json` / `sw.js` / `icons/`（`tools/gen_icons.mjs` で生成） |
| my_data | ブラウザの IndexedDB（`js/store.js`）。エクスポート/インポートあり |

---

## 11. 実装記録 v2（全データ取り込みとゲーム風UI）

### 11-1. データ取得（§5 の具体化）

- 参照サイトは日本語版 `jp.dblegends.net`。`tools/crawl_dblegends.mjs` が
  全キャラ（一覧＋個別ページ）・全装備（フラグメント）を間隔 2.5 秒で取得する。
  レジューム可能（取得済みはスキップ）。解析失敗ページの生HTMLは `game_data/crawl/failed/` に保存。
- キャラ個別ページには構造化JSONが埋め込まれている:
  `script#data`（min/max/soul ステータス、アビリティID、タグ）、`script#ab`（アビリティ原文）、
  `script#tr`（タグ名）、`script#eq`（装備可能装備一覧）。HTMLパースはほぼ不要。
- 装備ページは `eqd-slot`（SLOT別効果行）と `#eqdCharList`（**装備可能キャラの解決済み一覧**）を持つ。
  装備条件の AND/OR を自前で解析する代わりに `equip_char_ids` で判定する。
- **画像はダウンロードせずURLのみ保存**し表示時に参照する。
  公開リポジトリにゲームアートを再配布しないための判断。個人利用の範囲で参照する。
- 生HTMLの全量スナップショット（§5-4）は容量の都合で保存しない（±300MB になるため）。
  代わりに解析済みJSONキャッシュ（`game_data/crawl/`）と失敗ページのみ保存する。

### 11-2. データモデル v2

- キャラ: `stats` = Lv5000 の基本値（**❶ に相当。ブースト除く**）、`soul_max` = ソウルブースト最大値。
  合計ステ = stats + ユーザーのブースト値（my_data、初期値は soul_max）。
  旧形式 `base_stats`（合計ステ）も statBase() が後方互換で解釈する。
- 装備（フラグメント）: `slots[]`（SLOT別の効果行。値はレンジの**最大値**）、`star7`（★7解放スロット）、
  `equip_char_ids`、`rarity`（silver/gold/awakenedgold/platinum/unique）。
- アビリティ: 原文 `text` と解析済み `groups[]`（`cond` = ORリスト×ANDトークン列）を保存。
  条件トークンは `{tag:id}` / `{element:"RED"}` / `{name:"未解決"}`（未解決は一致しない＋警告）。
  属性・レアリティ・エピソードもタグ体系に含まれる（例: BLU=15004, HERO=12000, 劇場版編=20015）ため
  タグIDに解決できる。文言形式は3種:
  1. Zアビ形式: `{{ICN:ChaTag}}A or B` 条件行 ＋ `○基礎…X%` 行（空行でグループ区切り）
  2. 出撃Zアビ形式: `・基礎…をX%アップ`
  3. ZENKAI形式（インライン条件）: `バトル時、「タグ：X」または「属性：Y」の基礎…をX%アップ`
- アビリティのレベル I〜IV は限界突破の星から自動選択（>=6→IV, >=4→III, >=2→II, else I。
  **この対応は実機未検証** — キャラごとに z_level 等で上書き可能）。

### 11-3. 適用範囲の再定義（§2-2 の具体化）

| 補正源 | 発生条件 | 乗る対象 |
|---|---|---|
| Zアビリティ | パーティにいる | パーティ6体全員（タグ/属性条件に一致する者） |
| ZENKAIアビリティ | パーティにいる | パーティ6体全員（同上） |
| 出撃Zアビリティ（LL含む） | **発生源がバトルメンバー**（文言「自身がバトルメンバー時」） | バトル3体のみ |

### 11-4. UI（ゲーム編成画面風）

- 編成タブ: バトル3枠＋ベンチ3枠。キャラ選択・フラグメント選択はボトムシートのグリッド
  （画像・レアリティ枠色・装備中バッジ・残個数）。最適化結果は装備枠に直接反映され、手動でも変更可能。
- 未所持キャラ選択時は自動で所持登録（ブースト=ソウルブースト最大で初期化）。
- SLOT4（★7解放）は装備キャラの星が7未満なら計算から除外される。

### 11-5. 取り込みステータスの性質（実測との関係・重要）

参照サイトの `max` は **完全限界突破時の Lv5000 理論値**。§2-4 の検算キャラ
（DBL81-04S: 実機❶=231,537）に対しサイト値は 268,841 であり、**限界突破が最大でない
キャラでは実機とズレる**（ブースト値も同様に個人差がある）。

対応: my_data の `total_override`（実機のステータス画面の「合計ステ」を入力）が
最優先され、❶ = 合計ステ − ブースト値 で実測に合わせられる（§1-1 の手入力経路）。
未入力なら理論値でのプランニングになる。UI ではキャラ詳細シートの
「合計ステの実測値を入力」から設定する。

### 11-6. 全量取り込みの結果（2026-08-24）

- キャラ 704/704 体（詳細含む）・フラグメント 915/915 件・タグ 554 件を取得。
- 効果語彙は装備・アビリティ・条件式のすべてで**未解決ゼロ**
  （ステータス行 1,855 / 計算対象外 332 / 説明行 877）。
- レアリティ確定セット（config.json known_rarities）:
  iron / bronze / silver / gold / platinum / unique / event /
  awakenedbronze / awakenedsilver / awakenedgold / awakenedunique
- 特殊な装備ページ2形式に対応:
  効果なしスロット（eqd-empty）と選択式効果（eqd-option。レイドメダル系）。
  選択式はどれが付くか不定のため計算対象外とし原文表示のみ。

---

## 12. 実装記録 v3（実機ルールの反映と提案機能）

ユーザーからの実機仕様の聞き取りに基づく更新。§4-1 の当初仮定を上書きする。

### 12-1. フラグメント装備ルール（確定）

- 同一フラグメントは**同じキャラに重複装備できない**（常に強制。オプション廃止）
- **別のキャラ同士なら同じフラグメントを同時装備できる**（所持数の範囲で）
- 所持数の初期値は**6枚**（未設定時。フラグタブでタップして自由に増減）
- 初期値6ではパーティ最大6体×同キャラ重複不可のため奪い合いが原理的に発生しない →
  キャラごとに独立した厳密解（enumerateBest）。所持数を絞った場合のみ組合せ列挙＋分枝限定法
- 【力の大会】接頭辞のフラグメントは力の大会専用（`top:true`）。通常の最適化・候補から除外
  （トグルで表示可）。サイトに構造化マーカーが無いため名前接頭辞で判定

### 12-2. 効果条件（フラグメントの発動条件）

「バトルメンバーに[自身以外の]「タグ/属性/レアリティ/エピソード/キャラクター/バトルスタイル：X」が
[N人[以上]]いると、自身の〜アップ」形式をマージ時に解析（parseConditionalSlot、81行）。
- スコープは「バトルメンバー」= スタンダードはバトル3体、プラウドは自チーム3体
- 「N人いると」は **N人以上** と解釈（実機未検証の仮定）
- 「〜1人につき…%ずつアップ」形式は**人数×値**で適用（`cond_per_member`、対象91スロット）。
  「自身以外の」が付かない限り**自身も人数に数える**（実機未検証の仮定）
- パーティ文脈が無い計算（計算タブ等）では**適用しない**＋未適用として表示
- 発動系アビリティ文（「場に出た時〜発動する」等）は計算対象外のまま原文表示

### 12-3. リーダー枠の特殊ルール（確定）

左上のリーダー枠は「選出時に限り」:
1. 他の全キャラの**Zアビリティ**を「本来関係なくても」（タグを無視して）受ける
2. 自身の**Zアビリティ**を他の選出キャラに「タグを無視して」与える
- **対象はZアビリティのみ。ZENKAIアビ・出撃Zアビは無関係**（ユーザー確認済み）
- 条件一致で通常適用済みの分は二重適用しない
- 最適化時は「リーダーも最適化する」（既定ON）で全リーダー候補を総当たりし、
  最良のリーダーをリーダー枠へ自動移動する
- リーダー候補の優劣は各候補の最適化結果について**重み付き最終値（❸）の絶対合計**で比較する
  （候補ごとに補正の土台が変わるため、最適化内部の相対スコアでは比較できない）

### 12-4. バトル形式

- **スタンダード（3体選出）**: 上段3枠=バトル出撃、下段3枠=**ゼンカイ枠**。
  Zアビ・ZENKAIアビは6体全員からパーティ全体に乗る（§2-2 と同じ）。出撃Zアビはバトル3体のみ
- **プラウドバトル（6体全選出）**: 1戦目3体＋2戦目3体。同一キャラは両戦に選べない。
  アビリティ補正・効果条件は**チーム内3体で完結**（チーム跨ぎなし）。
  リーダーは各チームの先頭枠。3戦目（1・2戦目キャラを2体まで再選出可）はルール注記のみで最適化対象外
- ◎×N 関係数表示: 対象キャラに条件一致する（発生源×Z/ZENKAI種別）の組数。出撃Zは数えない。
  配置で数が増えたキャラにはパワーアップ演出

### 12-5. タッグキャラの2属性

タッグキャラは2属性を持ちうる（例: BLU+GRN）。キャラは両属性のタグ（15000番台）を持つため
タグ条件は自然に両属性へ一致し、`{element:}` トークン条件も `elements` 配列（タグから導出）で
両属性に一致させる。**どちらの属性でも色限定のZENKAI等が乗る**（ユーザー確認済み）

### 12-6. 所持アーツと提案機能

- キャラページ `script#ac` から所持アーツ（名前・種別: 打撃/射撃/必殺/特殊/究極/覚醒）を取得
- **提案A（長所）**: バトルメンバーのタイプ構成（打撃/射撃/援護/防御タイプ）から、
  指定目標と異なる「タイプに合った最適化」の結果を比較付きで提案（適用ボタンで反映）
- **提案B（ゼンカイ枠）**: バトル3体へのZ・ZENKAIアビ恩恵（重み換算）と、
  デッキに入る所持アーツ構成（必殺・特殊などレアアーツ／特化ステに合うアーツ枚数／体力恩恵）で
  ゼンカイ枠候補を4観点で提案
- 入手先（どこで手に入るか）表示は**参照サイトにデータが無いため未実装**

### 12-7. UI

ゲーム画面風のライトテーマに刷新（ハーフトーン背景・黒の斜めラベル・オレンジの斜めボタン・
戦闘力風の緑数字・シート/タブ/最適化反映/パワーアップの演出）。検索バーはスクロールしても
上部に固定（sticky）。キャラ・フラグメントとも既定で全件をカラー表示。

## 13. 検証記録（v2.1 リリース前レビュー）

多視点レビュー（正しさ/性能/UI/データ整合の16エージェント）で11件の実バグを検出し、全件修正済み:

1. リーダー探索が相対スコアで比較して逆転 → **絶対合計（重み×❸）比較**に変更
2. プラウドで空チームがあると teams/leaders の添字がずれる → 空チームの位置を保持
3. leaders の null が先頭キャラにフォールバック → null=リーダー無しの明示的意味に
4. 最適化の性能劣化（探索爆発） → 上限付き分枝限定（suffix-max 上界枝刈り）＋
   キャラ毎アイテム150件事前絞り込み＋リーダー候補間の itemsCache 再利用＋UIへの譲歩(yield)
5. 編成変更後も古い提案が適用できる → partySig による無効化
6. 「%アップする」等アビリティ文の一部形式が未解析 → 正規表現拡張＋節ごとの許容
7. 「1人につき」型の条件付き効果（91スロット）が常に0扱い → cond_per_member 対応
8. キャラID=0（孫悟空）が保存復元で消える → 空きスロットを null で表現
9. そのほかテスト期待値・表示崩れ等の小修正

テスト57件全通過。Playwright スモーク（編成→リーダー込み最適化 107ms→提案表示→
プラウド切替→最適化 76ms）通過。

## 14. v2.2: ゼンカイ枠自動選出・タイプ別特化・ULTRAアビリティ（実機フィードバック対応）

### 14-1. ZENKAI/Zアビの箇条書き型条件（最重要バグ修正）

- 「バトル時、「属性：X」かつ「タグ：Y」の以下のステータスをアップ」＋箇条書き形式
  （576グループ・144キャラ）と、【対象キャラクター】ブロック形式（・行=AND節、行間「または」=OR）を解析
- 属性・レアリティ・バトルスタイル等の条件もすべてタグ体系（GRN=15003, EXTREME=12001 など）で解決
- **フェイルセーフ（原則1-4）**: 効果があるのに条件らしき行が未解析のグループは
  「絶対に一致しない条件」を付けて警告する。無条件=全員適用という誤解釈を構造的に根絶
- 修正前はこの576グループが全キャラに乗っており、①無関係キャラへの幻のZENKAI補正
  ②❷の水増しで基礎なしフラグの過大評価、の2つの実害があった

### 14-2. ゼンカイ枠の自動選出（既定ON・トグルあり）

- `pickZenkaiMembers`: 所持キャラから「バトル3体への重み付き補正増分」で採点し上位3体
- 補正はキャラごとに独立な加算なので、上位3体=3枠合計の最大（厳密）
- リーダーの「Zアビをタグ無視で受ける」も採点に含むため、リーダー候補ごとに選び直す
- +1%の価値は 0.01×❶ で近似（フラグ配分が未確定の段階のため）
- 「基本は各色1体ずつ」は色限定ZENKAIの恩恵最大化の結果として自然に出る（ハードコードしない）
- 最適化のたびに選び直すため「別の最適化でゼンカイ枠が切り替わらない」問題も解消

### 14-3. タイプ別特化（styleSplit・既定ON）

- 攻撃の主軸がある目標のとき、主軸と逆のタイプ（打撃タイプ13002/射撃タイプ13003）のキャラは
  打撃/射撃の重みを入れ替えて組む（例: 射撃特化パの打撃タイプは打撃で組む）
- `optimizeParty` に weightsById（キャラ単位の重み上書き）を追加。奪い合いの裁定は
  同一スケール（主軸=1.0）のスコア合算で行う
- **逆転通知**: タイプ通りに組んだ主ステ最終値が、パーティ目標のまま組んだ場合より低いキャラは警告

### 14-4. ULTRAアビリティ

- キャラページ `d.ab.u`（[?,?,アビリティID,?]の配列）から取得。ULTRA 30体
- 与ダメージ・気力回復等の**戦闘効果のため❸には乗らない** → 原文と参照タグ（ref_tags）のみ保存
- 表示: キャラ詳細（参照タグのバトル3体内の充足数つき）、メンバーカードにULTRAバッジ
- リーダー探索のタイブレーク: 重み付き❸合計が最良の **0.5%以内** ならULTRA持ちをリーダーに優先
  （ULTRAアビはリーダー時に発動し❸に現れないため）。リーダー枠外のULTRA持ちには案内を表示

### 14-5. プリセット・UI更新

- プリセット5+1種: 完全打撃特化 / 打撃特化(総合重視) / 総合バランス / 射撃特化(総合重視) /
  完全射撃特化 / 耐久特化（旧attackプリセット保存値はbalanceへフォールバック）
- 覚醒フラグメント（rarityコード awakened〜、208件）は豪華な二重枠＋四隅飾りで区別
- 装備スロットのタップで詳細シート（条件達成状況・別フラグに変更・外す）。空きスロットはピッカー
- partySig に現在の重みを含め、目標変更で長所提案を自動失効させる

### 14-6. v2.2 リリース前レビュー（多視点+反証検証）

確定・修正済みの指摘:
1. **{{ICN:Epi}}単独の条件行が未解析**（62グループ・26キャラ）→ エピソード限定Zアビが
   全キャラに乗っていた。isConditionLine に Epi/Chara を追加して解決（例: ターレス→タグ20015）
2. フェイルセーフがICN形式を検知できない → ICN行はアイコン付き原文で unresolved に残し、
   CONDLIKE に {{ICN: を追加（未知の条件アイコンが将来増えても素通りしない）
3. targets=全員+ゼンカイ自動選出時、リーダー比較(absScoreOf)がベンチ自身のステで汚染
   → スタンダードではバトル3体のみ合算（ULTRAタイブレークも同基準に）
4. 自動選出ゼンカイ枠が weightsById/styleSplit の対象外で再実行結果が揺れる
   → タイプ別上書きを「確定したメンバー集合」からリーダー候補ごとに導出
5. pickZenkaiMembers が手入力の基礎なし補正(extNonBase)を無視 → 乗算項として採点に追加
6. 旧プリセット保存値のUI表示と実重みの不一致 → パネル描画時に balance へ正規化
7. ULTRAアビの空エントリ(111件)・レベル重複・敵対象タグ（「〜に対する」）の誤案内
   → クローラで除外・名寄せ・enemyフラグ化し、UIは敵対象を人数カウントしない
8. 逆転通知は奪い合いを考慮しない概算である旨を通知文に明記

## 15. v2.3: 全キャラ所持標準・ワンボタンデータ更新

### 15-1. 全キャラ所持を標準に

- `my_data.own_all`（既定 true）: 全キャラを所持として扱う。データタブでオフにすると従来の登録制
- 未登録キャラは **★7・ソウルブースト最大**の既定値（defaultCharMy）で計算。
  個別カスタマイズ（星・ブースト・実測値）は従来どおり charMy への登録で保存（ensureCharMy で遅延登録）
- ゼンカイ枠の自動選出は own_all のとき**全キャラが候補**になる。
  pickZenkaiMembers は abilityCorrections の全再計算をやめ、候補のアビリティが
  バトル3体に与える増分だけを直接計算する方式に書き換え（705体候補でも最適化全体 ~190ms）

### 15-2. ワンボタンデータ更新

- クローラに `--update` モード: 公開済み game_data/*.json をクロールキャッシュとしてシード →
  一覧を取得して**新規ページだけ**参照サイトから取得（既存700ページ超の再取得はしない）→ merge
- `.github/workflows/update-data.yml`（workflow_dispatch）: --update → テスト → 変更があれば
  main へコミット → gh-pages へミラー。アプリの「データ」タブのボタンからこの実行ページを開く
- 適用例: 最終形態クウラ（ULTRA, id 740）と新フラグ4件（1738/2093/2094/50210）を差分取得（5ページのみ）

### 15-3. 追加の条件形式・ULTRA表示

- 効果条件の新形式「**自身が「タグ/バトルスタイル：X」の場合、**」= 装備キャラ自身の静的条件
  （cond_scope:'self'）。50210 の打撃タイプ限定 +15% などが計算に入る
- 条件見出し行と解析済み効果行が順序混在するスロット（2093等）も、原文の並び順で
  見出し→後続行に条件を付与して解析（parseConditionHeader）
- ULTRAアビの参照タグは {{ICN:ChaTag}}X / {{ICN:Epi}}X 形式にも対応し、
  「敵…に」「…に対する」文脈は敵対象（enemy）として人数カウントから除外。
  表示時は ICN トークンを整形（タグ:/エピソード:/アップ）

## 16. v2.4: 覚醒排他・キャラ毎特化・総合ステ最大・条件未達回避

### 16-1. 覚醒前後の同一種フラグの排他

- フラグメントの「種」= アイコンID（覚醒版はベース版の `EqIco_<ベースID>` を共有。
  全209件中205件でベース実在、名前一致でも205件が検証済み。残り4件はベース未収録で排他は空振り）
- **同一種で覚醒/非覚醒が異なる2枚は同じキャラに同時装備できない**（fragsConflict）。
  最適化の列挙（enumerateBest / enumerateCombos）と手動ピッカー（無効表示）の両方で強制
- 力の大会版と通常版（同一種・どちらも非覚醒）は排他にしない（アイコン共有15件）

### 16-2. キャラ毎の特化切替

- メンバーカードに「特化」セレクタ: 自動（既定）/ パーティ目標 / 各プリセット
- my_data の characters[cid].objective に保存。手動指定は styleSplit の自動入替より優先され、
  逆転通知の対象にもならない。weightsById 経由で最適化・ゼンカイ選出・リーダー比較すべてに効く

### 16-3. 総合ステータス最大プリセット

- 全7ステータスの重み=1（❸の総和を最大化）。体力の絶対値が支配的になるのは仕様

### 16-4. 条件未達フラグの回避（既定ON）

- 「効果条件を満たせないフラグは選ばない」トグル: 現在の編成で条件未達の効果
  （conditionalOff）を1つでも持つ候補を最適化から除外する。⚠表示だらけの結果を防ぐ。
  未達分を無視した数値がそれでも高い装備を許すにはオフにする

## 17. v2.5: Zアビ条件の&連結引き継ぎ・スコアの絶対値化（実機フィードバック対応）

### 17-1. Zアビリティの&連結節の条件引き継ぎ

「バトル時、「タグ：X」の基礎体力最大値をN%アップ**&基礎クリティカル値をN%アップ**」のような
&連結文で、**2節目以降が条件を失い全キャラ適用**になっていた（桃白白・超ベジット・ヤジロベー・
チルド等）。実機では条件は文全体に掛かるため、後続節は直前の節の条件を引き継ぐよう修正。
表記の変遷: 旧={{ICN:...}}アイコン行+○効果行 / 新=「タグ：X」インライン文（どちらも対応済み）。
修正後、条件なしで効果を持つZ/ZENKAIグループは自爆与ダメ系（計算対象外）の6件のみ。

### 17-2. スコアの絶対値化と重みの再校正

- スコア = **Σ 重み × ❸（絶対値）** に変更。旧・相対値（❸/❸₀）は +X% の価値を全ステ同等に
  扱うため、総合ステ最大などの多ステ目標でクリティカル・気力回復のフラグ（絶対値は微小）を
  選ぶ「弱フラグ問題」の原因だった
- 副作用への対処（レビューで検出・再現確認済み）:
  1. プリセットの重みを絶対値スケールへ再校正（体力❶≈160万 vs 攻撃≈22万の桁差を補正。
     例: 打撃総合の体力 0.5→0.07）。クリティカル・気力回復は❸換算が微小なため多ステ
     プリセットから除外。カスタム重みも絶対値に掛かる旨をUIに注記
  2. weightsById（キャラ別特化）併用時の奪い合い裁定は、キャラ間で❸の桁が異なるため
     フラグ無し基準値❸₀で正規化してから合算（キャラ内の順位は不変）
- 基礎あり/なしの適用自体は §2-4 ケース(2) のフルパイプライン検算（❸=1,075,087.56）で担保済み

## 18. v2.6: 選択式スロット復元・装備可否の双方向化・除外トグル既定OFF

### 18-1. 選択式スロット（eqd-option）の欠落と復元

- 旧版パーサが選択式スロット（Option 1 of N / — OR — 区切り）を黙って落としており、
  95フラグでスロットが丸ごと欠番（例: 壁をさらに破る一撃のSLOT2）、244フラグが3スロット未満だった。
  キャッシュに固定されていたため疑い249ページを再取得して全欠番を解消（欠番0）
- 選択式はクロール時に raw+option番号で保存し、マージで選択肢ごとに条件付きスロットとして解析
  （276スロット中200が完全解析。残り76は気力回復・ドロップ数など非ステ効果で原文表示が正しい）
- **計算規則**: 所持個体にはどれか1つが付く。「条件を満たす選択肢のうち実効値合計が最大の1つ」を
  適用する（§3-4「所持品は最良個体」の前提に合わせる）。どれも未達なら未発動として表示

### 18-2. 装備可否の双方向化（新キャラ×既存フラグ）

- 装備可否はフラグ側の equip_char_ids だけで判定していたが、--update ではキャラページしか
  再取得しないため、**新キャラ（クウラ）が既存フラグをほぼ装備できなかった**
  （射撃フラグが実質3枚→装備プールが崩壊し「あの世にて+10%」等の弱装備の直接原因）
- マージでキャラページ側の equip_ids からフラグ側へ補完（双方向の和）。
  equip_char_ids が空（全キャラ可）のフラグはそのまま。クウラの装備プールは121件に回復

### 18-3. 「条件未達フラグを除外」トグルの既定をOFFに

- 旧既定ONの丸ごと除外は、未達条件を1行持つだけの強フラグ（例: ゴジータの射撃+35%）まで
  候補から消し、弱い装備の一因になっていた。未達の効果はもともと0価値で公平に評価されるため
  既定OFFへ変更（保存キーも excludeUnmetCond に改名し、旧保存値ONを引き継がない）。
  ⚠表示を完全に無くしたい場合のみ手動でON

## 19. v2.7: 選定理由の見える化（「なぜこのフラグ？」）

「弱いフラグが選ばれる」報告を実データで逆算検証した結果、報告された装備は
いずれも数理上の最適解だった（①基礎なし+12%は❷=183%環境で基礎あり+34%相当 ②キャラ個別の
耐久特化指定の最適解 ③選択式+条件込みで打撃38%相当）。額面%だけでは強さを判断できないため、
装備フラグ詳細に選定理由を表示する:
- **評価重みの由来**（個別指定/タイプ自動/パーティ目標）
- **この装備の寄与**（そのキャラの重み付き❸の増分）
- **候補トップ10比較**（他スロット固定で全装備可能候補を同条件採点し、順位を表示）
- 発動中効果の**基礎あり/なし注記**（基礎なしは「≒基礎+X%相当」の換算付き。§2-5の性質を明示）

## 20. v2.8: 装備ページ全件再取得（効果行の大規模欠落の修正）

実機スクショとの突き合わせで、**初回クロール時の旧パーサが同一スロット内の付加効果行
（プレーンな%行に続く条件付き行など）を黙って落としたままキャッシュに固定**されていたことが
判明（例: 我は不死身だっ！！！のスロット2/3の「自身以外の『タグ：未来/強大な敵』がいると
防御+12%」）。ローカルからは検出不能のため装備919ページを全件再取得した結果:
- **379フラグ（41%）で効果行が欠落**していた（行の純増のみ・減少ゼロ）
- うち132フラグで条件付き効果を回復（条件付き行 183→464）
- 残り未解決語彙は与ダメ・被ダメ系3語のみ → 計算対象外として effect_map に登録（未解決ゼロ）
- 教訓: キャッシュはパーサ改良後も古いパース結果を保持する。パーサを直したら
  影響範囲を再取得すること（今回で全ページが現行パーサ産に更新済み）

### 20-1. 1スロット内の連続する複数条件文（v2.8.1）

実機スクショとの突き合わせで、1スロットに条件文が2つ連続するフラグ（覚醒「かつての敵との
共闘！」スロット3: フリーザ軍→打撃攻撃 / 人造人間→打撃・射撃防御）で、**2つ目の効果に
1つ目の条件が付く**マージ解析バグを検出。条件見出し（バトルメンバーに/バトルメンバーの/
自身が）の開始位置で分割して個別解析するよう修正。全919件の再監査で該当6フラグを訂正
（かつての敵との共闘！・これが超サイヤ人3だ×2・8周年シャロット・待たせたな、カカロット・
オレをバカにしてるのか！）。行の増減はゼロ（条件の付け間違いのみ）

## 21. v2.9: 「総合強化重視」= 補正%の合計を最大化（percent プリセット）

「総合ステータス最大」を❸の絶対値合計で定義した結果、体力（❶≈160万）の+1%が攻撃の7倍以上の
価値になり、ゼンカイ枠自動選出も「体力%をくれるだけの非ZENKAIキャラ」ばかり選ぶ問題が出た
（ユーザー要望:「何%の補正がついているかを重視して評価してほしい」）。

- プリセットに `percent: true` を導入: 実行時にキャラごとに ❶ で正規化した重み
  （w[s] = 名目重み × 100000 ÷ ❶[s]）を生成し、**+1% をどのステータスでも等価**にする
  （= Σ ❸/❶ ≒ 補正%合計の最大化）。スコアエンジンは絶対値のまま、重みの生成だけで実現
- 旧「総合ステータス最大」→「**総合強化重視（補正%の合計を最大化）**」に改名・再定義。
  対象は主要5ステ（クリティカル・気力回復は%が安く稼げて評価が荒れるため除外）
- weightsById 経由で最適化・ゼンカイ枠選出・リーダー比較・フラグ詳細の寄与/比較すべてに一貫適用。
  実データ検証: フリーザ軍パのゼンカイ枠が「体力Z持ち非ZENKAI」→「フリーザ系ZENKAI×3」に改善
- キャラ個別の特化セレクタで「総合強化重視」を指定した場合も同様にキャラ別正規化が効く

## 22. v2.10: 条件未達フラグの控えめ減点 + 実戦バランスプリセット

ユーザーの実感（「今使ってるパーティの最適化が条件未達フラグが多くてモヤる」「基礎なしの
条件持ちフラグは全部発動した方が気持ちいい」）への対応。完全除外（excludeUnmetCond）は
強フラグまで候補から消すため既定OFFのまま、間に「控えめな優先付け」を追加した。

- **unmetPenalty（既定ON・0.95）**: 選出時、条件未達の効果行を1行持つごとにそのフラグの
  有効効果値を ×0.95 して評価する（`prepareItems` で base/nonBase 集計時に
  `Math.pow(unmetPenalty, 未達行数)` を乗算）。**実ステータス表示・❸計算には一切影響しない**
  選定用の重みダウン。僅差なら全発動フラグが勝ち、明確に強い未達持ちはそのまま残る
- 実データ検証（8パーティ・打撃総合重視）: 合計未達行 12→6 に半減、実スコア損失は最大 0.106%
- UI: 最適化パネルに「条件未達の効果を持つフラグを控えめに減点する」チェック（既定ON）。
  完全除外チェックとは独立に切替可能
- **新プリセット「実戦バランス（ゲームメタ重視）」**: percent ベース（§21）に、ゲーム内メタの
  参考評価で重みを傾けたもの。体力 1.15 / 打撃攻撃 1.1 / 射撃攻撃 0.95 / 打撃防御 1.15 /
  射撃防御 0.85（体力は高いほど良い・打撃は射撃よりダメージが高く打撃タイプが強い
  → 打撃防御が有利、というユーザー提供のメタ観を反映。あくまで参考値で調整前提）

## 23. v2.11: ◎×N（アビリティ関係数）の実機との食い違い — 真因は同名別バリアント

実機スクショ（GB=7 / ブロリー=8）とアプリ表示（GB=5 / ブロリー=6）の差分調査。
一度「リーダーのZアビをタグ無視で数える + ZENKAIは属性一致で数える」という規則を実装
したが（v15）、**これは数字が偶然合っただけの誤りだった**（ユーザー実機確認により棄却:
リーダーのタグ無視は選出時のみで編成画面の◎×Nには影響しない）。v16で全て取り消した。

**真因**: 同名別バリアントの取り違え。「最終形態 フリーザ：フルパワー」はゼンカイ持ちが
2体おり、アプリの自動選出は #219（防御型: Z=悪の系譜/フリーザ軍, ZENKAI=YEL∧悪の系譜）、
実機パーティは #414（射撃型: Z=強大な敵/フリーザ軍/Zフリーザ編, ZENKAI=YEL∧強大な敵）
だった。#414 で数えると従来の規則（条件の完全一致のみ・リーダー無視）のまま
実機表示(7/3/9/9/8/9)と6キャラ完全一致する。◎×Nのロジックに誤りは無かった。

- 教訓: 同名キャラのバリアント差（アビリティの対象タグ違い）は実機との照合で最初に疑う。
  数式仮説が「たまたま合う」ことがあるので、複数の独立した検証点（今回なら17号&GF=3）
  だけでなくユーザーの実機知識と突き合わせてから実装する
- ◎×Nは従来仕様のまま: 条件完全一致の（発生源×種別 z/zenkai）を数える。リーダー特例なし

## 24. v2.11: 変身後タグ（transform_tags）と装備条件の解析

変身キャラは変身前後でタグが変わることがある（実機仕様。例: ゴクウブラック#648 の
「神の気」は超サイヤ人ロゼ変身後に付与）。データ元サイトはこの区別をせず全タグを
一覧に載せるため、変身後タグを条件とする効果がアプリ上で常時発動扱いになっていた。
実機では: フラグは基本的に変身前にしか付けられない / 変身後にしかないタグが条件の
フラグ効果・アビリティは変身後のみ適用（=編成画面のステータスには乗らない）。

- **game_data/transform_tags.json**（手動管理の補正データ）: キャラID → 変身後タグID配列。
  store.loadGameData が読み込み時に character.tags から transform_tags へ分離する。
  以降の全判定（効果条件・アビリティ条件・◎×N・ゼンカイ選出・装備可否）が変身前タグ基準になる
- **装備条件の解析**: クロール済みの condition_text（「神の気 AND 射撃タイプ OR …」形式）を
  `parseEquipConditionText` でタグID条件（DNF）に解析し fragments.json の `equip_cond` に保存
  （919件中894件=条件テキストを持つ全件を解決。全角表記はNFKCで解決）。
  canEquip は transform_tags を持つキャラに限り、サイトの解決済みリストに加えて
  equip_cond を変身前タグで再判定する（通常キャラは従来どおりリストのみ → 挙動不変）
- 実データ検証: GB#648 は「神の気」条件の効果行4フラグが正しく未達扱いに変化。
  装備リスト側はサイトが元々正しく除外していた（「未来 OR 神の気」の2件は未来側で正当に装備可）。
  transform_tags を持たない全キャラの装備可否は完全不変を確認
- 新たな変身キャラ・タグが分かったら transform_tags.json に追記するだけで反映される

## 25. v2.12: 擬似ステータス「体力被回復量」と耐久プリセット強化

ユーザー要望「耐久特化の評価に体力被回復量も追加して。もしくは被回復量高めの耐久特化。
高い回復力を持つキャラがこの効果を持つ事で耐久力を上げられる」への対応。

- **擬似ステータス機構**（calc.js: PSEUDO_STATS / ALL_STATS / PSEUDO_STAT_BASE）:
  キャラの❶を持たない%効果を、仮想❶=100000 の仮想ステータスとして❸式に乗せて評価する
  （+1% = 重み×1,000点）。100000 は percent プリセットの正規化定数と同一なので、
  %等価プリセットでも重みをそのまま使える。実キャラのステ表示・計算タブには出さない
- **heal_received（体力被回復量）**を最初の擬似ステータスとして登録。effect_map で
  「体力被回復量」「被回復量」を {stat:'heal_received', base:false} にマップ
  （「体力回復量」= 与える回復量は別効果なので据え置き）。フラグ119件・Zアビ3キャラ
  （ネイルと融合ピッコロ/パン/チルド）が対象で、フラグとアビリティの被回復は加算で合算される
- アビリティ由来の被回復（base:false経路）は「未検証形式」警告の対象から除外
  （元々%加算の効果で基礎あり/なしの区別が無いため）
- プリセット: **耐久特化に heal_received 0.3**（僅差の後押し程度）、
  新設「**耐久特化（被回復量重視）**」は heal_received 1.0（+1% ≒ 防御+0.6〜0.8%相当）。
  キャラ個別の特化セレクタでも選択可
- 実データ検証: 防御持ち40キャラ中8キャラで被回復量重視により装備が変化（被回復+10〜12%）。
  寄与表示・候補トップ10・ゼンカイ枠選出も weights×detail.stats の共通経路で自動対応

## 26. v2.13: データ取り込みのアプリ内完結（GitHubへ飛ばない）

「新キャラ・新フラグ取り込み」ボタンが GitHub の Actions ページを開いて手動 Run workflow
を要求する体験を根絶した。

- **毎日 05:00 JST の自動取り込み**（update-data.yml に schedule cron を追加）。
  --update は一覧2ページ＋新規ページのみの取得で参照サイトへの負荷は僅少
- **アプリ内ワンタップ実行**: データタブで Fine-grained PAT（対象リポジトリ限定・
  Actions: Read and write のみ）を一度保存すると、ボタンが GitHub API の
  workflow_dispatch を直接起動 → 実行状況をポーリング表示 → 公開 meta.json の
  generated_at 変化で新データを判定（ワークフローは実変更が無いと meta をコミット
  しないため、変化 = 新データあり）→ SWキャッシュ更新と自動リロードまで完結する
- トークンは idb の 'gh_token' に端末ローカル保存。エクスポート（バックアップ）には
  含めない。未設定時は従来のリンクにフォールバック
- 進捗はタブ再描画をまたいで保持（dataUpdate モジュール状態 + #upd-status）

### 26-1. トークンの他端末引き継ぎ（v2.13.1）

エクスポート時にGitHubトークンを含めるか確認し（含めるのは希望時のみ・既定は含めない）、
含めたバックアップを他端末でインポートするとトークンも設定される。トークン無しの
バックアップをインポートしても既存トークンは消さない。store.exportAll({includeToken}) /
importAll の gh_token 復元で実装。

### 26-2. 仲間内利用（v2.14）

作者の要望でスコープを「個人利用」→「仲間内利用」に拡張した。アーキテクチャ変更は不要:

- アプリは元々公開URL（GitHub Pages）の静的PWAで、所持・編成データは各端末の
  IndexedDB に閉じている（利用者同士で混ざらない・サーバ不要のまま）
- **参照サイトへの負荷は利用人数に依存しない**: クロールは毎日1回、GitHub Actions が
  中央で実行するだけで、各利用者のアプリは GitHub Pages 上の JSON を読むのみ
- 手動取り込みボタンは「共有トークン」で仲間も同じ操作で使える:
  管理者が作った Fine-grained PAT（このリポジトリの Actions 起動のみ可能）を
  「トークンをコピー（仲間へ共有用）」で文字列共有 → 相手は初回設定に貼るだけ
  （GitHubアカウント不要）。漏れても可能なのは取り込みの起動だけで、
  concurrency 設定により多重実行もされない
- トークン未設定でも毎日の自動取り込みで全員のデータは最新に保たれる
  （未設定時のボタンはGitHubを開かず案内メッセージを出す）

### 26-3. 最終形: 手動取り込みボタンの廃止（v2.14）

作者の指示により、アプリ内の取り込みボタンとGitHubトークン機構（§26〜26-2）を撤去した。
現行仕様はこれが正:

- **自動取り込みのみ**: 毎日 14:30 / 15:10 / 18:00 JST（cron '30 5' / '10 6' / '0 9' UTC）。
  GitHub の cron は負荷で数分〜1時間程度遅れうる
- workflow_dispatch は管理者のGitHub上での手動実行用に残す（アプリからは使わない）
- アプリのデータタブは自動更新時刻の案内のみ。トークン保存・共有・エクスポート同梱は
  すべて削除（exportAll/importAll は my_data + game_overrides のみに戻した）
- 仲間は何の設定もなしに、アプリを開くだけで最新データになる

## 27. v2.15: 自動更新が毎回「新データなし」で空振りしていた（一覧ページの陳腐化）

毎日3回のスケジュール実行はすべて success なのに、9/5 を最後にデータが更新されなくなった。
ログの決定的な行:

```
キャッシュを公開データから復元: キャラ 705 体 / 装備 920 件
一覧を取得中…
キャラ 705 体 / 装備 920 件   ← 手元と同数 = 新規0件と判定
未取得 0 ページ
```

同時刻に手元から同じパーサで同じURLを取ると **キャラ 708 / 装備 923**。つまり
**GitHub Actions のランナーにだけ古い一覧ページが返っていた**（各取得が約0.1秒＝
キャッシュ応答の速さ。cf-cache-status は手元では DYNAMIC で、経路差によるもの）。
新キャラ・新フラグの発見は一覧ページが唯一の入口なので、ここが古いと永久に気付けない。

対策:
- `politeFetch(path, {fresh:true})` を追加。一覧ページのみ `Cache-Control: no-cache` /
  `Pragma: no-cache` / `?_cb=<時刻>` / `cache:'no-store'` で中間キャッシュを迂回する。
  詳細ページは新規URLでキャッシュされないため素のまま（origin への負荷を増やさない）
- **陳腐化ガード**: 取得した一覧が手元のデータ件数より少なければ即エラー終了する。
  キャラ・装備がサイトから減ることは無いので、これは取得失敗のサイン。
  「黙って新データなしと報告する」ことを防ぐ（原則 1-4 の精神）
- 教訓: 「success で終わる = 正しく動いている」ではない。件数の変化を必ずログに出し、
  変わらないことが続いたら疑う

## 28. v2.16: 既存キャラの「後からZENKAI覚醒」を取りこぼしていた／最終確認日時

「自動ZENKAI枠が変」「自動更新が止まってる」の2件を調査した結果:

### 28-1. 既存キャラページを二度と取り直さない設計の穴（実害あり）

`--update` は「まだ取得していないページ」しか取りに行かないため、**一度取得した
キャラページが永久に更新されない**。実機ではあとから ZENKAI 覚醒が追加されたり
アビリティが強化されたりするため、その分をまるごと取りこぼしていた。

実例: メカフリーザ(DBL50-02S)。サイトには ZENKAI アビ（PUR & 悪の系譜 /
基礎打撃攻撃+35・射撃攻撃+40・打撃防御+35・射撃防御+40）があるのに、手元は空。
一覧ページ由来の `zenkai` フラグは毎回更新されるので **フラグ true なのにアビが空**
という不整合として検出できた（708体中1体）。

対策（2段構え）:
- **不整合の即時再取得**: 一覧が「ZENKAI覚醒あり」と言っているのに手元に ZENKAI アビが
  無いキャラは毎回必ず取り直す
- **巡回再取得**: 毎回12体ずつ（`DBL_ROLL` で調整可）取り直し、全キャラが数週間で一巡する。
  アビリティ強化やステータス改訂にも自然に追随する

### 28-2. 「更新が止まっている」ように見えた件（実は正常）

9/9 以降データが変わらなかったのは、**サイト側に新規が無かった**ため
（同時刻に手元から数えても 708体/923件で完全一致）。しかし画面には
「取得日時（=最後にデータが変わった日時）」しか出ないため、止まったように見えていた。

- meta.json に **`checked_at`（最終確認）** を追加。`generated_at`（最終更新）は
  データが実際に変わったときだけ進める（merge 時に既存ファイルと内容比較して判定）
- ワークフローは meta.json だけの変化でもコミット・配信するようにした
  （コミットメッセージは「データ確認（新規なし）」）
- データタブに「最終更新 … / 最終確認 …」の2つを表示

### 28-3. ゼンカイ枠の選出そのものは正しかった

候補700体超について「実際にバトル3体の重み付き❸がどれだけ増えるか」を厳密計算し、
`pickZenkaiMembers` の選出と総当たり最良を突き合わせた結果 **完全一致（取り逃し0点）**。
テーマ的に無関係なキャラが選ばれて見えるのは、§12-3 の
**「リーダーは他の全キャラのZアビをタグ無視で受ける」** が効くため、
リーダー向けにはZアビの数値が大きいキャラほど有利になるのが理由（ルール通りの挙動）。
- 同点の候補が並ぶことがあるため、**完全同点なら ZENKAI 覚醒キャラを優先**するタイブレークを追加

## 29. v2.17: ゼンカイ枠「提案」が独自の誤った採点をしていた

実機スクショで、ゼンカイ枠に ブロリー：チライ / 荒野の大盗賊ヤムチャ / ヒット が
入っている状態を報告された（「ヤムチャはDBタグだから該当0のはず」）。

調査すると、枠を埋めたのは **自動選出（pickZenkaiMembers）ではなく「提案カードのセットボタン」**
だった（メッセージも「ゼンカイ枠を自動選出しました: 人造人間18号 / 魔人ブウ：無邪気 /
メタルクウラ」の直後に「ゼンカイ枠に提案メンバーをセットしました」と並んでいた）。

提案カードの採点には2つの欠陥があった:
1. **①アビリティ恩恵の採点が独自実装**で `重み × 効果%` の単純合算。❶の桁差を無視する
   相対評価（§17 で最適化側からは排除済みの誤り）で、リーダー特例も基礎なしも見ていない
2. **②必殺・特殊アーツ持ち／③打撃アーツ**の行は**アーツ枚数だけ**で並ぶため、
   ステータス恩恵が小さいキャラが「セット」で枠に入ってしまう

対策:
- `scoreZenkaiCandidates()` を optimizer から公開し、**提案も自動選出も同一エンジン**で採点する
  （`pickZenkaiMembers` はその上位3体。①行は自動選出と完全一致する）
- どの行にも「**この3体のステ恩恵: +X（最良は +Y）**」を併記
- 最良より劣る行の「セット」は確認ダイアログを出す

実データ検証（ブロリー：怒り(L)/17号&ゴールデンフリーザ/クウラUL・耐久特化）:
セットされていた3体は **+580,942**、最良の3体（最終形態フリーザ：フルパワー/魔人クウ/
メタルクウラ）は **+1,214,888**。半分以下だった。
なおヤムチャの恩恵が完全な0ではないのは §12-3 のリーダー特例（リーダーはタグ無視で
Zアビを受ける）によるもので、リーダー以外の2体には確かに何も乗らない。

教訓: 同じ概念（ゼンカイ枠の恩恵）を2か所で別々に計算しない。片方だけ直すと必ずずれる。

## 30. v2.18: キャラ詳細のアビリティ表示を「何が効いているか」まで出す

「アビリティの効果が変」との報告。超サイヤ人ゴッドSS界王拳 孫悟空(DBL98-01L/#738) の
キャラ詳細に Zアビが4行出ていた:

```
基礎打撃攻撃力 +38%（条件あり）
基礎打撃防御力 +38%（条件あり）
基礎射撃攻撃力 +18%（条件あり）
基礎打撃攻撃力 +30%（条件あり）   ← ★
```

取り込みデータの ZアビリティIV は 38/38/18 の**3件**しかない。+30% は
**ZアビリティIII の打撃攻撃値**で、通常の解決経路（`pickAbilityLevel` は1段だけ選ぶ）
では出ない。残る経路は `my.z_ability`（手入力オーバーライド、§1-1）で、
`memberAbilityGroups` が `manualToGroups` 経由で常に合算している。
アプリに手入力UIは無いため、古いバックアップや初期の検証データが残った可能性が高い。

問題は値そのものより **手入力と取り込みデータが画面上で区別できず、消す手段も無い**こと。
身に覚えのない補正が最適化にも効き続ける（原則 1-4 の「黙って間違える」に近い）。

対策（キャラ詳細のアビリティ補正欄）:
- 手入力由来は **【手入力】** と明示し、アクセント色で区別（`g.raw === '(手入力)'` で判定）
- **「手入力の補正を削除」ボタン**を追加（z/ll/zenkai の手入力を一括クリア）
- 条件を「（条件あり）」で済ませず **タグ名まで表示**（例: 条件: 宇宙代表 または 神の気 または サイヤ人）
- 出撃Zアビは発生源がバトル出撃中のときだけ有効なので、
  ベンチ（ゼンカイ枠）では **「※バトル出撃時のみ有効（現在は未出撃）」** と注記

## 31. v2.19: 「なぜこのキャラが選ばれたのか」をキャラ詳細で確認できるようにする

「またヤムチャがゼンカイ枠にいる」との報告。調査の結果 **これはバグではなかった**。

荒野の大盗賊 ヤムチャ(DBL31-03S/#328) のアビリティ:
- **Zアビ: 属性GRN または 属性YEL または エピソード:ドラゴンボール編 → 基礎打撃攻撃力+45% / 基礎打撃防御力+45%**
- ZENKAIアビ: 属性GRN かつ タグ:DB（こちらがDB条件）

報告時の編成は ガーリックJr.(PUR・リーダー) / 最終形態クウラ(**YEL**) / ブロリー：怒り(**GRN**) で、
**バトル3体中2体にZアビが正当に一致**していた。リーダー特例を完全に無視して採点しても
+155,902 の恩恵がある（特例込みで +235,382）。ユーザーは ZENKAIアビ側のDB条件を
Zアビの条件と読み違えていた。

再現: 打撃特化(総合重視)での上位は パーフェクトセル / 超サイヤ人3 孫悟空 /
超サイヤ人ゴッドSS ゴジータ で、報告画面の上位2体と一致。ヤムチャは僅差の4位で、
ゴジータが所持外なら3位に繰り上がる。

**本質的な問題は「選出理由が画面から分からない」こと**（同じ問い合わせが3回続いた）。
対策として、キャラ詳細に **「このパーティへの恩恵（ゼンカイ枠に置いた場合）」** を追加:
- 重み換算の合計（`scoreZenkaiCandidates` と同一の採点）
- バトル3体それぞれに何が何%乗るか。乗らない相手は「乗らない（条件を満たさない）」と明示
- リーダー特例（タグ無視）で乗っている分は「※リーダー特例で乗る分を含む」と注記

合わせて §30 の条件表示（タグ名を出す）により、条件の読み違いもその場で防げる。

## 32. v2.20: 星→アビリティレベルの実機対応表・未所持フラグ・耐久特化（総合重視）

ユーザー報告4件への対応。

### 32-1. 星→アビリティレベルの対応表が誤っていた（影響大）

§10-2 で「実機未検証の仮定」としていた対応が実機と違っていた。正しくは:

| 限界突破 | アビリティレベル |
|---|---|
| ★0〜2 | I |
| ★3〜5 | II |
| **★6〜13** | **III** |
| ★14 | IV |

旧実装は `★6以上→IV / ★4以上→III / ★2以上→II` で、**★7のキャラが全員 IV 扱い**
（本来は III）になっていた。既定値が★7のため、未登録キャラを含むほぼ全キャラの
Z・ZENKAI・出撃Zアビが過大評価されていたことになる。`autoAbilityLevel` を修正。

### 32-2. 「未所持にする」が全キャラ所持モードで効かなかった

`isOwned` は `own_all !== false || 所持登録あり` だったため、登録を削除しても
全キャラ所持が標準（既定ON）だと所持扱いのままで、最適化やゼンカイ枠の候補から外れなかった。

- `my.characters[cid].owned === false` を明示的な未所持として扱うよう `isOwned` を変更
- ボタンを「登録解除」→「**未所持にする**」に変更し、削除ではなく未所持フラグを立てる
- ゼンカイ枠候補（自動選出・提案の両方）を `isOwned` で絞る

### 32-3. 「耐久特化（総合重視）」プリセットを追加

純粋な耐久特化だと防御値の大きいZアビを持つキャラが勝ち、**ZENKAIアビ持ちが
選ばれなくなって編成として弱くなる**との指摘。攻撃も捨てない重みを追加した:
`{ hp: 0.1, strike_def: 1, blast_def: 1, strike_atk: 0.35, blast_atk: 0.35, heal_received: 0.3 }`

検証（クウラUL(L)/ガーリックJr./ブロリー：怒り＝全員「劇場版編」「強大な敵」）:
- 耐久特化: 上位2が ZENKAI無 の 伝説の超サイヤ人ブロリー#495 / ターレス#636
- 耐久特化（総合重視）: 上位3が **全て ZENKAI有**（ジレン：フルパワー /
  伝説の超サイヤ人ブロリー#207 / パーフェクトセル#432）

ユーザーの直感（劇場版編で揃えているならZアビ共有＋ZENKAIも乗る候補が上位に来るはず）は
正しく、原因は純耐久の重み付けだった。

## 33. v2.21: サイト内タグ（能力の詳細分類）

「キャラにはアビリティタグが付いているが、それだけでなくもっと詳細に絞り込みたい」
（例:「被弾時敵の手札破棄」「被弾時敵の気力減少」「被弾時敵にアーツコストアップ付与」
「被弾回数に応じて敵にデバフ付与」「控えに戻る時被弾回数に応じて敵にデバフ付与」
「遠距離系必殺」「近距離系必殺」「遠距離系特殊」「近距離系特殊」）への対応。

ゲーム本体のタグ（tags.json = 属性・エピソード・バトルスタイル等）とは別に、
**アビリティ本文から自動分類した「サイト内タグ」** を持たせる。名前空間を分けるのは、
ゲーム由来のタグに独自の値を混ぜると装備条件・効果条件の判定が壊れるため（§1-4）。

### 33-1. 変換表はデータに置く（§1-3）

分類の定義は `game_data/site_tags.json`。コードには正規表現を書かない。

```
{ "categories": [{ "id": "on_hit", "label": "被弾時" }, ...],
  "triggers":   [{ "id": "on_hit", "patterns": ["被弾時", "攻撃を受けた時", ...] }, ...],
  "effects":    [{ "id": "discard", "patterns": ["手札を[0-9]*枚?破棄", ...] }, ...],
  "tags":       [{ "id": "hit_discard", "category": "on_hit", "label": "被弾時に敵の手札破棄",
                   "trigger": "on_hit", "effect_any": ["discard"] }, ...] }
```

タグの作り方は3種類:
- `trigger` × `effect_any` … 同じ効果ブロック内でトリガと効果が両方出たら付く
- `effect_any` のみ … 本文のどこかに出れば付く（`any_*` 系）
- `structural` … 本文ではなく構造から判定（アーツ種別・ZENKAI・専用ユニフラ等）

`repeat: true` を付けたタグは、その効果ブロックが**回数依存**のときだけ付く
（`hit_debuff_stack` / `leave_debuff_stack`）。

### 33-2. アビリティ本文は2形式ある（最重要）

2025年の「孫悟空：少年期」(DBL82-03S) 以降とそれ以前で表記形式が違う。

| | 形式 | 例 |
|---|---|---|
| 新（DBL82以降） | `▼トリガー` + `○効果` の箇条書き | `▼場に出た時` / `○体力10%回復` |
| 旧 | 1文の散文 | `場に出た時、体力を15%回復` |

`parseAbilityBlocks()` が両方を `{trigger, body}` の列に正規化する。旧形式は
読点で区切って「前半＝トリガ、後半＝効果」とみなす。**トリガと効果を同じブロック内で
対応付ける**のが要点で、本文全体に対して正規表現を掛けると
「場に出た時◯◯」と「被弾時××」が混線する。

効果パターンは旧形式の助詞「を」を許容する必要がある（`体力(?:を)?15%回復`）。
これを忘れると旧形式のキャラが丸ごと分類から漏れる。

### 33-3. 何を本文として読むか（abilityCorpus）

- メインアビリティ（`d.ab.m`、今回クローラに追加）
- ユニークアビリティ / Zアビリティ / ZENKAIアビリティ / 出撃Zアビリティ
- アーツ詳細テキスト（`script#am`、今回クローラに追加）
- **専用ユニークフラグメントの効果行**

ユーザー要望どおり「専用ユニークフラグメントもユニークアビリティとして計上」する。
専用の判定は `exclusiveUniqueFragments()` = rarity が unique かつ
`equip_char_ids` がそのキャラ1体のみ。

### 33-4. アーツ種別と距離

アーツIDは `<接頭辞><キャラID>` で、接頭辞が種別を表す。

| 接頭辞 | 種別 |
|---|---|
| （なし） / `00` | 打撃 |
| `10` | 射撃 |
| `30` | 必殺 |
| `50` | 特殊 |

`11xxx` / `12xxx` は変身後の形態。キャラIDで終わらないものは共有アーツ。

距離は**データに明示がない**ため、アーツ詳細本文の「突進」の有無で推定する
（突進あり＝近距離系 / なし＝その場＝遠距離系）。ラベルにも
「（突進）」「（その場）」と書いて推定であることを示す。

**「後隙」はデータに一切存在しない**ため、「遠距離後隙なし特殊」は作れない。
代わりに実際にデータにある「射撃アーマー」を `armor_strike` / `armor_ultimate` として出した。

### 33-5. UI

- キャラタブのフィルタ内に「能力で絞り込む（サイト内タグ）」を追加。系統ごとに見出しを付け、
  各タグに該当数を併記。**1体も該当しないタグは出さない**（空振りする選択肢を並べない）
- カテゴリ内は**該当数の少ない順**。ほぼ全キャラが持つタグ（体力回復537等）は
  絞り込みに効かないので後ろへ回る
- 複数選択は AND。一覧の上に「表示中 N 体（全 708 体中）」を出す
- キャラ詳細に「能力タグ（サイト内分類）」を系統ごとに表示

### 33-6. 途中で見つかった2件のUIバグ（サイト内タグとは独立）

1. **`el('details', { open: false })` が開いてしまう** … `el()` のブール属性リストに
   `open` がなく `setAttribute('open', false)` → 属性 `open="false"` は HTML では
   「存在する＝開いている」。`checked`/`disabled` と同じくプロパティ代入に変更
2. **フィルタのチップが押しても選択状態にならない** … チップの `class` は生成時に
   1度決まるだけで、押しても一覧しか再描画されないため色が変わらなかった
   （レアリティ・属性など既存チップ全てに影響）。チップ自身の塗り直し関数を
   `refreshers` に登録し、どれか1つ押したら全部塗り直す（「獲得済み/未獲得」のような
   排他チップも正しく戻る）。サイト内タグの `— N件選択中` も同じ仕組みで更新する

### 33-7. 結果

全 708 体中 677 体にタグが付いた（46タグ / 10系統）。ユーザーが挙げた例はすべて再現:

| 要望 | タグ | 該当 |
|---|---|---|
| 被弾時敵の手札破棄 | `hit_discard` | 29 |
| 被弾時敵の気力減少 | `hit_ki_drain` | 42 |
| 被弾時敵にアーツコストアップ | `hit_cost_up` | 21 |
| 被弾回数に応じて敵にデバフ | `hit_debuff_stack` | 15 |
| 控えに戻る時、被弾回数に応じて敵にデバフ | `leave_debuff_stack` | 3 |
| 近距離系必殺 / 遠距離系必殺 | `ult_melee` / `ult_ranged` | 177 / 454 |
| 近距離系特殊 / 遠距離系特殊 | `sp_melee` / `sp_ranged` | 58 / 566 |

重点対象（DBL82以降59体 + ZENKAI177体 + 専用ユニフラ75体 = 延べ257体）は
**全員にタグが付き、46タグすべてが1件以上該当**する。

### 33-8. 回数依存の判定（ハマりどころ）

当初 `hit_debuff_stack` が1体しか該当しなかった。実際の表記を調べると:

- 「受けた**回数に応じて**」… 【1回目以上】【3回目以上】と段階が並ぶ形式
- 「（3回）」… 発動回数の上限。N≥2 なら回数依存
- 「※交代時、発動回数リセット」… これは**注記であって回数依存ではない**

を踏まえて `REPEAT_RE = /ずつ|回数に応じて|ごとに|度に|たびに|\([2-9]\d*回\)/` とし、
15体 / 3体が正しく取れるようになった。テストで3形式とも固定している。

## 34. v2.22: 全キャラに「ULTRA」が付いていた／タッグキャラの2属性

ユーザー報告2件。「ドーレ＆ネイズだけ優遇されてるのはなんで？」「なんでULTRAが着いてるの？」

### 34-1. ULTRAバッジが全キャラに付いていた（バグ）

メンバーカードのバッジ判定が `(character.ultra_ability || []).length` だった。
しかし `ultra_ability` は**ULTRA専用ではなく、全キャラが持つ「ユニーク系アビリティ」**
（スターターアビリティ・ユニークアビリティ・特殊カバーチェンジ・ユニークゲージ・
力の共鳴 など）で、実データでは **707/707 体が保有**している。

| レアリティ | ultra_ability 保有 |
|---|---|
| HERO | 44/44 |
| EXTREME | 164/164 |
| SPARKING | 457/457 |
| LEGEND | 11/11 |
| ULTRA | 31/31 |

クローラ側のコメントには「フィールド名は互換のため ultra_ability のまま」と
書いてあったが、UI側がその前提に追従していなかった。

影響は表示だけに留まらなかった:

1. **メンバーカードのバッジ** … 全員に ULTRA が付く（報告された症状）
2. **リーダーのULTRA優先タイブレークが死んでいた** …
   `if (!(best.leaders[0].ultra_ability || []).length)` は常に偽になるため、
   「❸合計が最良の0.5%以内ならULTRAキャラをリーダーに優先する」が一度も動いていなかった
3. **案内メッセージの氾濫** … 「○○はULTRAアビリティ持ちです」がバトルメンバー全員分出る
4. **キャラ詳細の見出し** … 全キャラのユニークアビリティが「ULTRA ウルトラアビリティ」表記

いずれも判定を `rarity === 'ULTRA'` に変更。キャラ詳細の見出しは
ULTRA なら「ULTRA ウルトラアビリティ」、それ以外は「ユニークアビリティ」に出し分ける。
フィールド名 `ultra_ability` は my_data オーバーライドとの互換のため維持し、
コメントで意味を明記した。

### 34-2. ドーレ＆ネイズが優遇されて見えたのは仕様（バグではない）

報告時の編成で ◎×9（他は◎×6〜8）。再現して原因を特定した。

**ドーレ＆ネイズはタッグキャラで属性が2つ（GRN + RED）**。ゼンカイ枠3体の
ZENKAIアビリティが、たまたま全部「属性＋劇場版編」条件だった:

| 発生源 | 条件 | サウザー(PUR) | ドーレ＆ネイズ(GRN+RED) |
|---|---|---|---|
| 最終形態 クウラ[zenkai] | RED & 劇場版編 | ✗ | ✓ |
| 超サイヤ人3 孫悟空[zenkai] | GRN & 劇場版編 | ✗ | ✓ |
| 勇者 タピオン[zenkai] | RED & 劇場版編 | ✗ | ✓ |

Zアビリティ6件は両者とも同じく乗るので、差はこのZENKAIアビ3件ちょうど＝◎×6と◎×9。
`conditionMatches` は `elements` 配列のどちらでも一致させる実装で、実機どおり。
装備可能フラグメント数はむしろドーレ＆ネイズの方が少ない（69件 vs サウザー75件）ので、
フラグメント側の優遇は無い。

2属性キャラは13体（すべてタッグキャラ）。属性条件のアビリティを二重取りできるのが
タッグキャラの強みであり、**そう見えるのが正しい**。

### 34-3. タッグ・変身キャラのタグが重複していた（表示のみ）

ドーレ＆ネイズのタグは `クウラ機甲戦隊/男/SPARKING/援護タイプ/GRN/劇場版編/ドーレ/
クウラ機甲戦隊/男/SPARKING/援護タイプ/RED/劇場版編/ネイズ` と**2人ぶん並んでいた**。
変身キャラも形態ぶん重複する。**90体 / 707体**が該当。

判定は全て `includes`（集合）なので**数値には一切影響しない**
（重複除去の前後で◎×Nが変わらないことを確認）。ただしキャラ詳細のタグ表示が
重複するため、クローラの `merge()` で `[...new Set(...)]` して落とすことにした。

### 34-4. 不完全な my_data でキャラ詳細が落ちる（堅牢化）

調査中に発見。`boost` などのキーが欠けた my_data エントリがあると、
`labeledNum(..., m2.boost, s)` が `undefined` を読んで**キャラ詳細を開いた瞬間に落ちる**。
仲間内でエクスポート／インポートをやり取りするようになった以上、
古い版や手編集のファイルを読む可能性がある。`ensureCharMy` が既存エントリについても
欠けたキーを既定値で埋めるようにした。

## 35. v2.23: 自動更新のたびにアーツ系タグが消えていた（公開データの往復漏れ）

§33 のサイト内タグを入れた直後、GitHub Actions の自動更新が走った結果を見ると
タグ付与が **677体 → 653体** に減り、**636体でタグが減っていた**。

### 35-1. 原因: 判定材料が公開データに載っていなかった

失われたタグの内訳は `arts_special:595 / arts_ultimate:594 / sp_ranged:551 /
ult_ranged:446 / ult_melee:169 …` と、ほぼアーツ系。

自動更新（`--update`）は GitHub の使い捨てランナーで動くためクロールキャッシュを持たず、
`seedCacheFromData()` が**公開データ（game_data/*.json）からキャッシュを復元**する。
ところが `arts_detail`（アーツ本文）は「大きいので公開データには出さない」方針で
キャッシュにしか無かった。つまり:

```
クロールキャッシュ（arts_detail あり） → 公開データ（arts_detail なし）
                                        → 復元キャッシュ（arts_detail なし）→ タグ消失
```

**判定材料は公開データを往復できなければならない**、という一般則を踏み外していた。
`arts_detail` を公開データに含めるようにした（characters.json 4.87MB → 5.78MB、+0.91MB）。

### 35-2. 「本文が無いから0件」を正常として通さない

§1-4（未知の語を0として扱わない）と同じ穴だった。本文が欠けても
「タグ0件」は正常終了に見えるため、誰も気づかないまま公開されていた。
マージ時にアーツ本文の保有率を出し、9割を切ったら**エラーで止める**ようにした。

```
サイト内タグ: 677 体に付与（定義 46 種） / アーツ本文あり 708 体
```

回帰テストも2本追加した（公開データが arts_detail を持つこと／
アーツ系タグが実際に大量に付いていること）。

### 35-3. 自動更新との衝突の解消手順（記録）

この修正中、main には自動更新のコミットが2件入っていた。dev にマージする際、
`game_data/*` は素直に取り込めないので次の手順で解消した:

1. 自動更新版と自分の版を**フィールド単位で差分**を取り、
   `site_tags` / `arts_detail` / `tags`（重複除去）以外に差が無いことを確認
2. 装備4件に差（スロット内の行の**並び順だけ**、内容は同一）があったので、
   新しい方（自動更新版）をローカルのクロールキャッシュに書き戻す
3. `--merge` で再生成し、自動更新版のキャラ・装備が1件も欠落していないことを確認

「自動更新の結果を捨てて自分の版で上書きする」のは、たとえ内容が同じに見えても
やらないこと。差分を取って、新しい方をキャッシュに取り込んでから再生成する。

## 36. v2.24: ゼンカイ枠の恩恵が1体に偏る — 「3体に行き渡らせる」選び方を追加

「ZENKAI3枠が全部一体に付くのはなんで？ 複数のZENKAIを一体に付けた方が基礎なしの
数値が上がって結果的に全キャラ合計スコアが高くなるから？ それよりかは、
全キャラを強くして欲しいかな」という指摘への対応。

### 36-1. まず事実確認: バグではなく、目的関数が「合計」だから

報告の編成（クウラUL / サウザー / ドーレ＆ネイズ）で再現した。

- **既存の選出は「合計」については厳密に最適だった。** 上位80候補の総当たり
  82,160通りと突き合わせて差分 0。独立採点の上位3体を採る近似は正しかった
- **「基礎なしが乗算で効くから集中する」わけではなかった。** この編成で
  Z/ZENKAIアビ由来の基礎なし効果は全候補・全メンバー合わせて3回しか出現せず、
  補正はほぼ純粋な加算（❸ は ❷ に対して線形）
- つまり原因は「誰が受け取るかを問わず合計だけ見ている」目的関数そのもの
  （なぜ偏るのかの正確な理由は §36-6。当初「2属性だから1枠から多く貰える」と
  説明したが、これは誤りだった）

実際の受取内訳（耐久特化・総合重視）:

| | 最終形態 クウラ | サウザー | ドーレ＆ネイズ |
|---|---|---|---|
| 合計最大化 | +290,042 | +266,621 | **+763,325** |

### 36-2. 目的関数: 「一番伸びない1体の伸び率」を最大化する

`ui.opt.zenkaiBalance` で選び方を切り替える。

- **合計最大化（既定・従来どおり）** … Σ 増分
- **3体に行き渡らせる** … min(増分/❶) を最大化、同点なら合計が大きい方

**伸び率（増分/❶）で比べる**のが要点。絶対値で比べると ❶ の大きいキャラばかり
優遇されて、結局「行き渡らない」。

### 36-3. 貪欲法は使えない（実測で確認）

最初は log(1+伸び率) の和（幾何平均＝比例公平）を貪欲法で解いたが、3プリセット中
2つで**結果が合計最大化と1体も変わらなかった**（伸び率 0.3〜1.0 の範囲では
log の凹みが弱すぎる）。

min の最大化に変えても、**貪欲法は局所解に落ちた**。最初の1体を「最小値だけ」で
選んでしまうためで、総当たりと比べて合計・最小の**両方が悪い**という結果になった:

| 解き方 | 合計 | 最小伸び率 |
|---|---|---|
| 貪欲 maximin | +1,299,814 | 50.3% |
| **総当たり maximin** | **+1,706,474** | **65.9%** |

そのため探索は総当たりにした。全候補（705体）の C(705,3) ≈ 5,800万通りは無理なので、
**プールを絞る**:

> 各バトルメンバーを単独で最も伸ばす上位K体の和集合 ＋ 合計上位K体（K≈30）

「最小を上げる」解は各メンバー単独の上位にしか現れないので、合計順の上位だけを
プールにすると取りこぼす。実測でプールは 90〜120体（12万〜28万通り）、
**108ms/回**（合計最大化は 12ms/回）。ボタン押下時の処理としては許容範囲。

### 36-4. 効果（実データ）

| プリセット | 合計の変化 | 一番伸びないキャラの変化 |
|---|---|---|
| 総合バランス | −3.1% | **+53.2%** |
| 打撃特化（総合重視） | −4.0% | **+47.2%** |
| 耐久特化（総合重視） | −16.3% | **+33.6%** |

総合バランスでの受取内訳は +614,223 / +358,090 / +789,464 →
**+597,299 / +548,598 / +560,577** とほぼ揃う。合計はわずか3%の犠牲。

### 36-5. 注意

提案カード（§29）の採点は従来どおり「合計」ベースのまま。バランスは
**3体の組合せに対する評価**で、候補1体ごとの点数には落とせないため
（候補単体のランキングとしては合計基準が妥当）。

### 36-6. 偏る本当の理由は「1枠あたりの取り分」ではなく「狙える候補の数」

当初 §36-1 で「ドーレ＆ネイズは2属性なので属性条件のアビリティを両取りするから
集中する」と書いたが、これは誤りだった。ユーザーからの指摘:

> 2属性と言っても掛けられるZENKAIアビはZENKAI一枠につき一個なんだから、
> そこで差がつくのはなんで？ 悟空3の「劇場&緑」を紫変身ブロリーの「劇場&紫」と
> 交換してもZENKAIはサウザーに効くから関係なくない？

**指摘のとおり**。1枠から貰える ZENKAI アビは1つで、交換すれば宛先は移せる。
実データで確認すると、1枠あたりの価値はほぼ互角だった（総合バランスの重み）:

| 宛先 | 最良の候補 | 価値 |
|---|---|---|
| サウザー(PUR) | 超サイヤ人 ブロリー(DBL07-07S) [PUR&劇場版編] | +206,076 |
| ドーレ＆ネイズ(GRN+RED) | スーパージャネンバ / 超サイヤ人3 孫悟空 | +212,933 |

差はわずか3%。「両取りで倍もらえる」ような構造的な差は無い。

**本当の差は候補の数**。全705体のうち、そのメンバーに ZENKAIアビを当てられる候補は:

| メンバー | 属性 | 狙える候補 |
|---|---|---|
| 最終形態 クウラ | YEL | 8体 |
| **サウザー** | **PUR** | **2体** |
| ドーレ＆ネイズ | GRN+RED | 10体（GRN 4 + RED 6・重複なしの純加算）|

タグではなく属性が効いている。検証として、サウザーのタグのまま属性だけ
GRN+RED にすると 10体、ドーレのタグのまま PUR にすると 2体と、きれいに入れ替わる
（※属性名は tags 配列にも属性タグIDとして入っているため、この手の反実仮想を
試すときは tags からも属性タグを抜かないと汚染される。最初その罠に落ちた）。

結果として:
- サウザーは**枠が3つあっても最大2つしか受け取れない**（そもそも2体しか存在しない）
- ドーレ＆ネイズは10体から選べるので3枠すべてを向けられる

### 36-7. 勝敗を決めていたのは ZENKAIアビではなく Zアビだった

さらに追及の指摘:

> つまり候補が多いからよりZアビが合致したZENKAIキャラの強いのが選ばれやすかった、ということ？

**そのとおり**だった。候補1体の寄与を Zアビ分 / ZENKAIアビ分 に分解すると:

バランス選択が入れ替えた1枠（合計 −55,304）の内訳:

| | Zアビ分 | ZENKAIアビ分 | 合計 |
|---|---|---|---|
| 超サイヤ人3 孫悟空(DBL48-01S) → ドーレ | +363,353 | +212,933 | +576,286 |
| 超サイヤ人 ブロリー(DBL07-07S) → サウザー | +314,906 | +206,076 | +520,982 |
| **差** | **−48,447** | −6,857 | −55,304 |

**差の88%はZアビ由来**で、ZENKAIアビの差は12%（6,857）しかない。

各プールの中身を見るとさらにはっきりする。ZENKAIアビ分はどのプールでも
+205,000〜+225,000 とほぼ一定なのに対し、Zアビ分は +76,871〜+387,577 と5倍も開く:

| 狙う相手 | 候補数 | Zアビ分の最大 | ZENKAI分の最大 |
|---|---|---|---|
| 最終形態 クウラ(YEL) | 8体 | +363,353 | +224,960 |
| サウザー(PUR) | **2体** | **+314,906** | +206,076 |
| ドーレ＆ネイズ(GRN+RED) | 10体 | **+387,577** | +212,933 |

つまり ZENKAIアビは「どれを選んでも同じくらいの固定ボーナスを1体に配る」役でしかなく、
**実際の勝負は全員（＋リーダーはタグ無視で全部）に乗る Zアビの強さで決まっている**。
候補プールが大きいほど「Zアビが強い1体」を引ける確率が上がるので、
候補10体のドーレ狙いが候補2体のサウザー狙いに勝つ、という構図だった。

バランス選択の代償が合計3%程度で済むのは、失うのが ZENKAIアビの差ではなく
「Zアビがもう少し強い候補を諦める」分だけだからである。

## 37. v2.25: 与ダメージを「最終火力への乗算」として評価に入れる

「悟空3（劇場版）のZアビリティには孫一族に打撃アーツ与ダメージが付いてて、
これは恐らく最終火力に乗算だと思う。調べて、ちゃんと付与して」への対応。

### 37-1. 取り込みはできていたが、計算から捨てていた

データ自体は正しく入っていた。超サイヤ人3 孫悟空(DBL48-01S) の ZアビリティIV:

```
「タグ：孫一族」の打撃アーツ与ダメージを3%アップ
 & 「タグ：孫一族」または「エピソード：劇場版編」または「タグ：サイヤ人」の
   基礎打撃・射撃攻撃力を38%アップ
```

条件も正しく分離されていた（与ダメージは**孫一族のみ**、基礎攻撃力は3タグのOR）。
問題は effect_map で `"打撃アーツ与ダメージ": {"other": true}` としていたこと。
「計算対象外と確認済み」の箱に入れていたので、黙って捨てていた。

規模: **フラグメント79件 / キャラ13体**が与ダメージを配っている。無視できない。

### 37-2. ❸ に混ぜてはいけない（実機のステータス画面と一致しなくなる）

与ダメージは ❸（最終ステータス）と同じ乗算の形をしているので、
`{stat:"strike_atk", base:false}` と書けば既存の基礎なし補正の経路にそのまま乗る。

**しかしそれをやると ❸ が実機のステータス画面と合わなくなる。**
§2-4 で検算した「❸＝ゲームのステータス画面の値」という不変条件が壊れる。
与ダメージはステータス画面には出ない、ダメージ計算時の倍率だからである。

そこで entries に4つ目の形を足した:

```
{ "stats": ["strike_atk"], "base": false, "damage": true }
```

- **最適化スコア**では基礎なし補正と同じ乗算項として効かせる
  （`makeScoreContext` で `extNonBase + damage` として合算。探索ループにも
  分枝限定の上界計算にも手を入れずに済むので、厳密解の保証が壊れない）
- **表示**では `characterDetail` が `damagePct` に分離し、❸ には含めない。
  メンバーカードに「火力 637,242（与ダメージ +3%） ※ステータス表示には含まれません」
  として別行で出す
- アビリティ由来の分も `ext.damage` という専用バケツに入れ、`extNonBase` を汚さない
  （「基礎なし補正は未検証形式」の警告も出さない。与ダメージは元々%乗算の効果なので）

### 37-3. どの文言を対象にしたか（§1-3: 変換表はデータに置く）

打撃/射撃のどちらに乗るかが**一意に決まるものだけ**を対象にした:

| 文言 | 対象 |
|---|---|
| 与ダメージ / 自身の与ダメージ | strike_atk + blast_atk |
| 打撃与ダメージ / 打撃アーツ与ダメージ | strike_atk |
| 射撃与ダメージ / 射撃アーツ与ダメージ | blast_atk |

**`other` のまま残したもの**と、その理由:

- 必殺与ダメージ / 究極与ダメージ / 究極・覚醒与ダメージ …
  そのアーツを撃つ時だけ乗り、アーツ種別も打撃/射撃どちらもありうるため
  打撃・射撃のどちらに配るか決められない（データ上の出現数は最多。今後の課題）
- 「〜に対する与ダメージ」（特攻）… 相手依存
- 味方の自爆による与ダメージ / 自身が敵を戦闘不能にすると与ダメージ … 状況限定

### 37-4. 影響（実データA/B）

与ダメージ付きフラグを装備できるのは 84/707 体。その84体で最適装備を比較:

| プリセット | 装備が変わったキャラ |
|---|---|
| 完全打撃特化 | 8/84 |
| 総合バランス | 43/84 |

専用ユニークフラグメントには +300%（SPARKING!! - ザッハ）のような大きな値もあるが、
装備できるのが1体だけなので奪い合いを歪めない。

### 37-5. 前提（実機未検証）

「与ダメージ+X% は攻撃力+X% と同じだけ最終火力を伸ばす」と仮定している。
実際のダメージ式は攻撃力と防御力の関数なので厳密には比例しない。
ユーザーの見立て（「恐らく最終火力に乗算」）に沿った一次近似であり、
画面に与ダメージ%を明示しているので、合わないと分かれば係数を調整できる。

## 38. v2.26: ZENKAIアビリティを星から切り離し、ZENKAIレベルで決める

「赤の最終形態クウラを★13にしたらZENKAIアビが3になった」という報告。

> ZENKAIアビリティは本体の凸が星7以上で、ZENKAIソウルの取得によってZENKAIレベルが
> 上がります。そのため星7でZENKAIレベルが7、ZENKAIアビリティが4という場合もあります。

### 38-1. 何が間違っていたか

§32 で直した星→アビリティレベルの対応表（★0-2:I / 3-5:II / 6-13:III / 14:IV）を、
Zアビ・出撃Zアビだけでなく **ZENKAIアビリティにも使い回していた**。
そのため ★13 では ZENKAIアビリティが III 止まりになっていた。

実際には**ZENKAIアビリティは星とは連動しない**:

- ZENKAI覚醒の前提が本体★7以上（ここだけ星が関係する）
- そこから先は **ZENKAIソウル**の取得で **ZENKAIレベル**（1〜7）が上がる
- したがって「★7のまま ZENKAIレベル7 = ZENKAIアビリティIV」がありうるし、
  逆に「★14 でも ZENKAIレベル1 = アビリティI」もありうる

星は ZENKAI の進み具合を何も表していなかった。

### 38-2. 対応表（ユーザー提供）

| ZENKAIレベル | ZENKAIアビリティ |
|---|---|
| Lv1〜2 | I |
| Lv3〜4 | II |
| Lv5〜6 | III |
| Lv7 | IV |

※ユーザーの回答は「1-2,3-5,5-6,7という感じ」で Lv5 が2つの帯に跨っていたため、
4帯を1〜7に均等に割る自然な読みとして **3-4 / 5-6** と解釈した。
`optimizer.js` の `ZENKAI_ABILITY_BY_LEVEL = [1,1,2,2,3,3,4]` 一箇所を直せば変えられる。

### 38-3. 実装

- `zenkaiAbilityLevel(zenkaiLv)` を新設（星依存の `autoAbilityLevel` とは**別関数**）
- `ZENKAI_MIN_STARS = 7`。★6以下では ZENKAIアビリティを発動させない
- my_data に `zenkai_lv`（ZENKAIレベル 1〜7）を追加。**未入力は Lv7＝最大**として扱う
  （ユーザー指定。ZENKAI覚醒しているなら育て切っている前提）
- キャラ詳細に「ZENKAIレベル」セレクタを追加。ZENKAIアビリティの自動表示も
  「自動（★13 → III）」から「**自動（Lv7 → IV）**」に変わる
- 手入力の上書き（`zenkai_level`）と無効化（`zenkai_level = 0`）は従来どおり効く

★6以下のキャラでは、セレクタの代わりに
「限界突破が★7未満のため無効です」と理由を出す（黙って0にしない — §1-4）。

### 38-4. 注意: 3種類のアビリティでレベルの決まり方が違う

| アビリティ | レベルの決まり方 |
|---|---|
| Zアビリティ | 限界突破（星）— §32 の表 |
| 出撃Zアビリティ | 限界突破（星）— §32 の表 |
| **ZENKAIアビリティ** | **ZENKAIレベル（ZENKAIソウル）— §38 の表** |

同じ「アビリティレベル」でも根拠が別なので、`autoAbilityLevel` を
ZENKAI に流用しないこと。回帰テストで両者が別物であることを固定してある。

## 39. v2.27: 「%は高いのに合計が低い」— 換算の重みが見えていなかった

「なんでこれでタピオンの方が数値が高いの？ Zアビはクウラの方が高くて、
打撃与ダメージ（最終に乗算）も付いてるのに」という指摘。

### 39-1. スコアは「%の合計」ではなく「重み換算後の合計」

指摘のとおり、素の%だけ見ればクウラの方が高い。

| | 1体あたりの素の% |
|---|---|
| 勇者タピオン Zアビ | 基礎打撃**攻撃**+28 / 基礎打撃**防御**+28 = 56 |
| 最終形態クウラ Zアビ | 基礎打撃攻撃+32 / 基礎**射撃攻撃**+32 = 64（＋打撃与ダメージ+2） |

ZENKAIアビは両者とも同一（RED&劇場版編 → 40/35/40/35、ドーレ＆ネイズにのみ一致）。

差が出るのは**重み**。実データで全プリセットを総当たりしたところ、
**耐久系プリセットのときだけタピオンが勝つ**:

| プリセット | タピオン | クウラ |
|---|---|---|
| 総合強化重視 | +318,000 | **+350,720** |
| 実戦バランス | +342,000 | **+359,392** |
| 打撃特化（総合重視） | +470,471 | **+489,618** |
| 完全打撃特化 | +326,555 | **+381,396** |
| 総合バランス | +525,085 | **+647,790** |
| **耐久特化（総合重視）** | **+406,297** | +365,616 |

耐久特化（総合重視）の重みは `strike_def:1 / blast_def:1 / strike_atk:0.35 / blast_atk:0.35`。
1体あたりで換算すると:

- タピオン: 28×0.35（攻撃）＋ 28×**1.0**（防御）＝ **37.8**
- クウラ: 32×0.35 ＋ 32×0.35（どちらも攻撃）＝ **22.4**

**タピオンのZアビは半分が防御**なので耐久重みを丸ごと受け取り、
**クウラのZアビは全部が攻撃**なので 0.35 に割り引かれる。素の%が高くても、
重みの小さいステータスに偏っていると合計は伸びない。

打撃与ダメージ+2% も、耐久プリセットでは strike_atk（重み0.35）にしか掛からないので
ほとんど効かない。攻撃系プリセットなら効く。

※報告の2枚のスクリーンショット（+432,512 / +411,415）は、キャラデータからは
どのプリセットでも同時に再現できなかった。撮影時刻が1分違い、片方だけ
プリセットを変えた後だった可能性が高い。**これが見えないこと自体が問題**なので、
下記のとおり換算に使った重みを常に表示するようにした。

### 39-2. 直した表示3件

1. **与ダメージが「打撃攻撃力 +2%」と表示されていた**（§37の表示漏れ）
   `${e.base ? '基礎' : ''}${STAT_LABELS[e.stat]}` で組み立てていたため、
   与ダメージが「基礎なしの打撃攻撃力補正」に見えていた。ステータス画面に出ない値を
   ステータス補正と誤解させるので、`effectLabel()` を作って
   **「打撃与ダメージ」「射撃与ダメージ」**と出すようにした
2. **ZENKAIアビの条件が「不明かつ劇場版編」と表示されていた**
   条件の属性指定には**2形式**がある（§36-6でも踏んだ罠）:
   - `{tag:15000, name:'RED'}` … タグID形式（勇者タピオン）
   - `{element:'RED'}` … 属性形式（最終形態クウラ）
   表示側が `tk.name || tk.tag` しか見ておらず、属性形式が「不明」に落ちていた。
   `condTokenLabel()` で両形式を扱う。判定（`conditionMatches`）は元々両対応なので
   **計算は正しく、表示だけが誤り**だった
3. **恩恵カードに換算の重みを併記**
   「重み換算の合計」の下に「換算に使った重み: パーティ目標（…）。下の%は素の数値で、
   合計はこの重みを掛けた後の値です。重みの小さいステータスに偏った補正は、%が
   大きくても合計は伸びません」を追加。%と合計が食い違って見える理由をその場で示す

## 40. v2.28: ゼンカイ枠の逆引き・キャラ一覧のアビリティ数値・理論値モード

要望3件:

> - そのキャラに適用可能なZENKAI覚醒済みキャラ一覧が欲しい。なんなら、適用されたら
>   何がどのくらい上がるかみたいな表示付きで
> - ZアビリティやZENKAIの数値がキャラ欄で簡単に分かるように
> - お遊びとして、ゲーム内で最も〇〇の数値が高くなるキャラ&フラグ&パーティの
>   組み合わせを作成みたいなモードが欲しい

### 40-1. 共通の土台: sumGroupsFor

3機能とも「あるキャラに、あるアビリティが乗ると何が何%上がるか」を必要とするので、
`sumGroupsFor(groups, target, ignoreCond)` に切り出した。
`ignoreCond` はリーダー特例（§12-3: リーダーは全Zアビをタグ無視で受ける）と、
一覧表示用の「素の数値」の両方に使う。戻り値は base / nonBase / damage を分けて持つ
（§37: 与ダメージはステータス補正ではない）。

### 40-2. ゼンカイ枠の逆引き（キャラ詳細）

`zenkaiProvidersFor(target, characters, effectMap)` … ZENKAI覚醒キャラのうち、
**そのキャラに ZENKAIアビリティが乗る**ものだけを、効果の内訳つきで返す。

実データでの例（§36-6 の分析と一致）:

| 対象 | 属性 | 候補 |
|---|---|---|
| サウザー | PUR | **2体** |
| ドーレ＆ネイズ | GRN+RED | 10体 |

候補が2体しかいないキャラは、ゼンカイ枠で伸ばすこと自体が構造的に難しい。
これが一目で分かるのがこの表の価値。Zアビ分も併記する
（§36-7: 実際の優劣を決めているのは ZENKAIアビより Zアビであることが多いため）。

### 40-3. キャラ一覧にアビリティ数値（キャラタブ）

- フィルタに「アビリティ数値を表示」トグル。タイルに `Z+85 / ZK+90` のように出す
  （ツールチップで内訳）
- 並び替えに「Zアビの数値」「ZENKAIアビの数値」「アビリティ合計」を追加

表示する数値は**条件を無視した素の合計**。誰に乗るかは編成次第なので、
一覧では「そのキャラが配れる最大量」として出している。

707体ぶん毎回解決すると重いので、`キャラID:星:ZENKAIレベル:手入力レベル` をキーに
キャッシュする。

### 40-4. 理論値モード（計算タブ・お遊び）

「ゲーム内で最も○○の数値が高くなるキャラ＆フラグ＆パーティ」を探す。
全キャラ所持・★14・ZENKAIレベル7・フラグメント無制限が前提。

**厳密解は不可能**（707体から6体 × フラグメント配分）なので2段構え:

1. 各キャラを**リーダー兼対象**に固定して ❷ を求める。
   リーダーは全キャラのZアビをタグ無視で受ける（§12-3）ので、
   **Zアビ分は対象に依らず一定** → 先に計算して使い回せる。
   条件判定が要るのは ZENKAI（177体）と出撃Z（98体）だけなので速い。
   枠割りは「出撃Zアビは出撃3体からしか出ない」を考慮した近似
   （出撃Z込みで強い2体をバトル枠、残りをゼンカイ枠）
2. 上位12体だけフラグメントを厳密最適化して ❸ を出す

実測 **0.3秒**。近似であることは画面の method 行に明記する。

**与ダメージは計算から外す**（§37）。外さないと、与ダメージ+170%の専用ユニフラを持つ
超サイヤ人ブロリー(DBL74-03S) が 5,465,974 で1位になってしまう。これは
「打撃攻撃力の数値」ではなく火力なので、質問の答えとして誤り。除外すると
上位5体が 2,069,181〜1,996,950 と僅差で並び、理論値の天井らしい結果になる。

結果例（打撃攻撃力）:

| 順位 | ❸ | キャラ | 補正 |
|---|---|---|---|
| 1 | 2,069,181 | 超サイヤ人ゴッドSS ゴジータ(DBL30-01S) | +503% |
| 2 | 2,050,588 | 最終形態 クウラ(DBL51-01S) | +489% |
| 3 | 2,012,511 | 半身崩壊 合体ザマス(DBL35-08S) | +483% |

どの編成にも「ネイル」が入るのが面白い所で、Zアビの数値が全キャラ中でも高く、
リーダー特例でタグ無視で乗るため、対象が誰であっても候補に上がる。

## 41. v2.29: 編成プリセットと「所持0フラグメント」の一覧

### 41-1. 編成プリセット

これまで編成は「今いじっている1つ」しか保存できず、別構成を試すと前のが消えていた。

**自動保存（`parties[0]`）とプリセット（`party_presets`）を分けて持つ**設計にした。
自動保存の経路（`syncPartyToMyData` / `restorePartyFromMyData`）はそのままなので、
既存の挙動は一切変わらない。

```
my_data
  parties: [ … ]        … 今いじっている編成。今までどおり自動保存される
  party_presets: [ … ]  … 名前を付けて保存した編成（§41で追加）
```

シリアライズは `currentPartySnapshot(name)` / `applyPartySnapshot(p)` に切り出して
両方で共用する。保存内容はキャラ6体・装備・バトル/ゼンカイ枠・表示ステータス・
最適化の設定一式。`equips` と `opt` は**ディープコピー**する
（参照を共有すると、プリセット保存後に編成をいじった分がプリセット側にも及んでしまう）。

UIは編成タブ上部に選択セレクタ＋「読込 / 新規保存 / 上書き / 名前変更 / 削除」。
読込・上書き・削除は確認ダイアログを挟む（今の編成を失う操作のため）。

`my_data` に入るので、データタブのエクスポート／インポートにそのまま乗る。
旧データ（`party_presets` が無い）を読んでも `emptyMyData()` が空配列を補うので落ちない。

### 41-2. 所持0にしたフラグメントの一覧

所持数0のフラグメントは一覧でグレーアウトされてはいたが、**923件の中から探す手段が
無かった**。「これから作る物」を確認したいという用途に応えて、絞り込みを追加:

- チップ「所持しているもの」「**所持0のもの（N）**」（Nは件数を常時表示）
- 見出しを「表示中 3 件（全 923 件中）／ 所持0にしたもの 3 件。」に変更

キャラ一覧（§32 の「未所持にする」）と同じ考え方で、
**所持していない物を「見えないように消す」のではなく「絞り込んで見せる」**方針に揃えた。

## 42. v2.30: 「なぜこのキャラ？」の答えが候補プールだった

> なんで緑の超サイヤ人3の悟空が？ 確かにZアビは高いけど、赤2枚なら赤の共通タグ上げの
> ZENKAI持ち（赤の半身崩壊合体ザマス）とか入れた方が強くね？

### 42-1. 指摘は正しかった。ただし採点のバグではない

報告の編成（シャレット(GRN,L) / ゴジータUL(RED) / ベジットUL(RED)）を再現し、
**全キャラを候補にして**採点し直すと:

| | 全キャラ中の順位 | 恩恵 |
|---|---|---|
| 超サイヤ人3 孫悟空(DBL10-01S,**RED**) | **1位** | +894,746 |
| **半身崩壊 合体ザマス(DBL35-08S,RED)** | **2位** | **+825,825** |
| 超サイヤ人4 ゴジータ(DBL35-01S,GRN) | 10位 | +486,557 |
| 超サイヤ人3 孫悟空(DBL17-05S,**GRN**) | 12位 | +475,010 |

ザマスの ZENKAIアビは `RED & 神の気` 条件で、**バトル3体のうち2体（ゴジータUL・
ベジットUL）に乗る**。指摘どおり、入れられるなら入れた方が強い。
重み設定を6プリセット × styleSplit有無の12通りで試したが、
**ザマスは全12通りで上位3に入り、緑の超3悟空は1通りも入らなかった**。

それでもアプリが緑を選んだ理由は**候補プール**だった。自動選出の候補は:

```
全キャラ所持がON  → 所持扱いの全キャラ
全キャラ所持がOFF → my_data に「登録済み」のキャラだけ
```

登録済み6体だけを候補にして再現すると、**画面と同じ3体がそのまま出た**。
つまり採点は正しく、ザマスはそもそも土俵に上がっていなかった。

`pickZenkaiMembers` が合計について厳密最適なのは §36-1 で総当たり検証済みなので、
**最適化の品質は入力（候補プール）で決まる**。

### 42-2. 「何体から選んだか」を必ず出す

原因がプールだと画面から一切分からなかったのが本当の問題。次を追加した:

1. **自動選出メッセージに候補数を併記**
   `ゼンカイ枠を自動選出しました（所持登録済みの 3 体から選出。未登録のキャラは候補に入りません）: …`
2. **候補が全体の2割未満なら警告**
   `※候補が 3/708 体しかありません。強いZENKAI覚醒キャラを持っていても、
   所持登録していないと選ばれません。`
3. **提案カードに「候補外だが、入れれば上位に来るキャラ」**
   プール外を採点し直し、現在の3位より強いものを最大5体、**その場で所持登録できる
   ボタン付き**で出す。再現環境では先頭が
   `+825,825 半身崩壊 合体ザマス（DBL35-08S,RED）` になり、
   ユーザーの指摘がそのまま画面に出る

### 42-3. 教訓

§29 で「提案の採点が独自実装でズレていた」を直し、§36 で「目的関数が合計だけ見ていた」を
直したが、今回は**採点も目的関数も正しいのに答えが悪い**ケースだった。
最適化結果を疑うときは、アルゴリズムの前に**入力の範囲**を表示すること。
「N体から選んだ」は、それ自体が答えの一部である。

## 43. v2.31: 「総合重視」の意味を直し、ULTRAアビリティ「力の共鳴」を計算に入れる

### 43-1. 「打撃/射撃 特化（総合重視）」は反対側の攻撃を計上しない

> 射撃総合重視や打撃総合重視の時、相対するアーツは計上しないか評価を下げるみたいなこと
> できないかな。正確に言うと「射撃総合重視」なら射撃を高めつつ打撃と射撃の防御と体力を
> 上げたい、みたいなイメージ

旧: `{ blast_atk: 1, strike_atk: 0.15, hp: 0.07, strike_def: 0.5, blast_def: 0.5 }`
新: `{ blast_atk: 1, strike_atk: 0,    hp: 0.09, strike_def: 0.9, blast_def: 0.9 }`

重みは「Σ 重み × ❸（絶対値）」の空間なので、❶の桁を掛けた「+1%あたりの点数」で見る
（ゴジータUL の ❶ で換算）:

| | 旧 | 新 |
|---|---|---|
| 射撃攻撃 +1% | 2,796 | 2,796 |
| **打撃攻撃 +1%** | **384** | **0** |
| 打撃防御 +1% | 850 | **1,530** |
| 射撃防御 +1% | 850 | **1,530** |
| 体力 +1% | 1,860 | **2,391** |

反対側の攻撃に 0.15 が入っていたため、射撃しか要らない場面で打撃だけ上げるフラグや
アビリティが僅差で勝つことがあった。0 にしたうえで防御・体力を引き上げ、
「主軸を伸ばしつつ、硬さも取る」という名前どおりの意味にした。

### 43-2. ULTRAアビリティ「力の共鳴」

> リーダー枠なら無条件で与ダメ気力回復30%、それ以外ならパーティの指定のタグの人数で
> 5%ずつ与ダメ気力回復上昇。例えばこのゴジベジは力の共鳴が合体戦士なので合体戦士で
> 組むと強いはず

データを確認すると**31体（ULTRA全員）が保有**し、表記は5種類あるが意味は2系統:

| 形 | 体数 | 内容 |
|---|---|---|
| リーダー節あり（新▼○表記・旧散文の両方） | 28 | リーダー 30% / 非リーダーはタグ1人につき 5%ずつ |
| リーダー節なし（旧EVT版） | 3 | タグ1人につき 10〜15%ずつ・上限あり |

パターンは `effect_map.json` の `_ultra_resonance` に置いた（§1-3: 変換表はデータに置く）。
**31/31 体を読めることを確認済み**。リーダー節と非リーダー節を先に切り分けてから数値を
拾うのが要点で、混ぜると 30% と 5% を取り違える。

**計算への組み込み**: 与ダメージぶんを §37 の damage チャンネルへ入れる。
`partyAbilityCorrections` は編成画面・最適化の両方が通る唯一の場所なので、そこで加算する。
ステータス表示(❸)は汚さず、火力とスコアにだけ効く。
気力回復速度は effect_map で `other`（ステータスではない）なので加えない。

**表示**:
- 編成画面に「力の共鳴」サマリ（誰が何%受け取っているか・合計）
- キャラ詳細に「タグ○○が N 人 → +X%」と**該当メンバー名**、
  さらに「リーダーに置いた場合」との比較

### 43-3. 報告された2編成の比較（そのまま再現）

| 編成 | 合体戦士 | ゴジータUL | ベジットUL | 合計 |
|---|---|---|---|---|
| ① 超サイヤ人3 孫悟空 ×2 入り | 4人 | +20% | +20% | **+40%** |
| ② 超ゴジータ / 半身崩壊 合体ザマス 入り | 6人 | +30% | +30% | **+60%** |

「2枚目のパーティの方がウルトラアビが行き渡る」という指摘が、そのまま数値で出る。
①は射撃攻撃の ❸ が高いが、②は力の共鳴が +20% ぶん多い。
**ステータスだけ見ていると②が劣って見えるが、火力では差が縮む**という構図。

## 44. v2.32: 与ダメージをステータス計算から外す（§37 の撤回）

> 与ダメに関してはちょっと複雑で、多分ユニークアビリティやユニークフラグメントにおける
> 「与ダメージ」と「ダメージガード」はかさむんだと思う。だからウルトラアビリティの30%upも、
> ダメージが3割上がるんじゃなくて各与ダメUP%の合計にさらに30%上乗せする、というだけだと思う。
> なので多分与ダメージupはステータス計算に入れなくていい。

### 44-1. §37 の判断を撤回する

§37 で「与ダメージ＝最終火力への乗算」としてスコアに入れ、`❸ × (1+与ダメージ%)` を
「火力」として表示していた。実機を知るユーザーの指摘により**撤回**する。

理由:
- 与ダメージ／ダメージガードは**全ソースで加算される1つのプール**であって、
  ステータス（❸）とは別の軸。`+X% 与ダメージ` と `+X% 攻撃力` は等価ではない
- ULTRAの +30% も「ダメージが3割増える」のではなく、プールに30を足すだけ
- 敵側のダメージガードも同じプールに関わるため、こちら側の数値だけでは
  実際のダメージを出せない。**半端に式を作るより、入れない方が正しい**

### 44-2. 変更点

**計算から外した場所**（`e.damage` を持つ効果を採点で読み飛ばす）:

| 場所 | 用途 |
|---|---|
| `prepareItems` | フラグメント最適化の候補採点 |
| `makeScoreContext` | ❸ スコアの乗算項（extNonBase への合算をやめた） |
| `scoreZenkaiCandidates` / `zenkaiEffectTable` | ゼンカイ枠の自動選出 |
| `theoreticalMax` | 理論値モード |

**表示は残す**（情報としては有用）:
- メンバーカード: `与ダメージ +250% ※ステータスとは別枠（加算で積み上がる戦闘補正）。計算には含めていません`
- **`火力` の数値表示は廃止**。`❸ × (1+与ダメージ%)` は実機の式とは別物なので、
  数字にすると精度があるように見えてしまう（`characterDetail` の `effective` も削除）
- 「力の共鳴」（§43）のサマリと内訳はそのまま。ただし**スコアには入れない**

検証: 与ダメージ付きフラグを装備できる84体で、
**「与ダメージしか無いフラグ」が選ばれた数は 0 件**になった。

### 44-3. ついでに「総合重視」の重みを測り直した

§43 で 防御 0.5→0.9 / 体力 0.07→0.09 に上げたが、実データ50体で測ると
**主軸の攻撃が -11% も落ちていた**。「主軸を伸ばしつつ」という元の要望に反する。

| 防御 | 体力 | 主軸攻撃 | 防御合計 | 体力 |
|---|---|---|---|---|
| 0.5 | 0.07（旧） | 100% | 100% | 100% |
| **0.6** | **0.08** | **96.6%** | **106.8%** | 99.5% |
| 0.7 | 0.08 | 94.2% | 111.3% | 98.5% |
| 0.9 | 0.09 | 88.8% | 115.7% | 101.0% |

**0.6 / 0.08 を採用**（主軸 -3% で防御 +7%）。反対側の攻撃を 0 にするのは維持。

### 44-4. 教訓

§37 はユーザー自身の見立て（「恐らく最終火力に乗算」）に沿って実装したもので、
本人が後から精度を上げて撤回を指示した。**ゲーム内部の式は当てにいかず、
確かな部分（ステータス画面と一致する ❸）だけを計算し、
不確かな部分（与ダメージのプール）は数値化せず情報として出す**という切り分けに戻す。
§1-4「未知の語を0として扱わない」と同じ精神で、
**分からないものを分かったように数値化しない**。
